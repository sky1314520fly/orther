Outcome: PASS by design/unit/resource audit. Formatting is daemon-first through the existing packaged lsp-daemon client; CLI fallback is single-shot, bounded by timeout, SIGKILL, and shared per-path single-flight. No formatter daemon or resident formatter process is created. The live generic Senpi driver reported realSenpiUntouched=true and comment-checker PASS; no raw process dump is retained.