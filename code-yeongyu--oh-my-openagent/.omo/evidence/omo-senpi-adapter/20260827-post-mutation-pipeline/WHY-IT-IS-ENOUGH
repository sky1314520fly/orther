The unit evidence proves the requested deterministic policy and extraction contracts; the full Senpi gate exercises the adapter’s real test surface; the live driver proves isolated runtime wiring and preserves the real Senpi agent directory. The implementation reuses the packaged daemon for formatting first and only falls back to bounded single-shot CLI execution, with per-path single-flight and no new resident process path.