# F1 Plan Compliance Audit Rerun - memory-v2

**Result: FAIL**

Audit basis:

- Binding checklist: `/tmp/memv2/musthave.md`
- Full plan: `.omo/plans/memory-v2-active-learning.md`
- Settled source HEAD before this evidence commit: `af2f66c1b`
- Prior F1 evidence: `.omo/evidence/omo-senpi-adapter/memory-v2/F1/compliance.md`
- Remediation commits inspected: `aa7ef94e4`, `47f7c863f`, `0baef0c1f`, `7e4e0c505`, `feb6faa3a`, `af2f66c1b`
- Audit method: static source, packaged-asset, generated-schema, and commit-order inspection. Source files were not modified. A pre-existing worktree modification to `packages/omo-senpi/plugin/extensions/omo.js` was left untouched.

Verdict vocabulary follows the task: **DELIVERED**, **MISSING** for an unmet Must-have, and **VIOLATED-PRESENT** for a forbidden Must-NOT behavior that remains present.

## Must-have rows

| Row | Requirement | Verdict | Current evidence |
|---|---|---|---|
| MH-1 | Exact config defaults; all feature kill switches wired end-to-end; regenerated schema | **DELIVERED** | The schema carries the required default-on reflection/nudge/facts/dream/people/soul values and exact numeric defaults (`packages/omo-config-core/src/schema/memory.ts:7-19,38-80,161-190`), and the generated schema matches (`assets/omo.schema.json:1233-1462`). The shared absent-root helper resolves through `OmoMemorySettingsSchema.parse({})`, not a disabled ad hoc tree (`packages/omo-senpi/src/components/memory/identity-runtime.ts:25-30`). Nudge and soul use it (`packages/omo-senpi/src/components/memory/wiring.ts:37-55`), facts uses it for enablement/debounce (`packages/omo-senpi/src/components/memory/wiring-runtime.ts:73-85`), dream uses it before identity override resolution (`packages/omo-senpi/src/components/memory/wiring.ts:89-96`), palace uses it (`wiring.ts:113-119`), and command settings use it instead of throwing (`wiring.ts:122-125`). Automatic reflection sessions now carry resolved `enabled` (`packages/omo-senpi/src/components/memory/wiring-runtime.ts:107-127`), and trigger wiring clears pending compaction and launches no automatic work when false (`packages/omo-senpi/src/components/memory/trigger-wiring.ts:99-116`). `dream.idle_minutes <= 0` prevents arming the idle timer (`packages/omo-senpi/src/components/memory/dream-trigger.ts:123-129`). |
| MH-2 | Reflection step-count default 25; explicit `step_count:0` disables | **DELIVERED** | Default 25 and nonnegative acceptance are explicit (`packages/omo-config-core/src/schema/memory.ts:7-14,167-174`; `assets/omo.schema.json:1251-1273`). The machine requires `threshold > 0`, so an explicit zero disables the step trigger (`packages/memory-core/src/reflection/machine.ts:122-130`). |
| MH-3 | Soul v2 seed, optional identity, `<self>` v2, template bump, three notice surfaces | **DELIVERED** | The v2 persona invites self-evolution and carries the announcement instruction (`packages/memory-core/src/seeds/default-memory.ts:26-47`). Optional `system/identity.md` is read and rendered beside persona inside `<self>`, while both are excluded from generic system rendering (`packages/memory-core/src/compile/compile.ts:9-10,38-49,81-115`). The template is v2 (`packages/omo-senpi/src/components/memory/prompt.ts:11`). Soul writes unconditionally append the tool-result discipline line (`packages/memory-core/src/tools/memory.ts:85-96`; `packages/memory-core/src/tools/memory-apply-patch.ts:80-92`; directive definition at `packages/memory-core/src/soul/paths.ts:14-15`), the visible non-model-facing entry is gated by `edit_notice` (`packages/omo-senpi/src/components/memory/soul-notice.ts:46-64`), and reflection-delta metadata is rendered (`packages/memory-core/src/compile/compile.ts:122-136`). |
| MH-4 | Nudge line + REMINDER v2 + memory-discipline skill; no duplicated rule across surfaces | **MISSING** | The exact REMINDER and nudge metadata are present (`packages/memory-core/src/compile/compile.ts:11-14,122-136`), and the skill is seeded (`packages/memory-core/src/seeds/memory-discipline.ts:18-24`; `packages/memory-core/src/seeds/seeds.ts:55-72`). The single-carrier rule is still not met: persona instructs, “If you change this file, tell the user” (`packages/memory-core/src/seeds/default-memory.ts:45-47`), while the skill independently commands, “Never edit them silently” and explains the same user-visible drift rationale (`packages/memory-core/src/seeds/memory-discipline.ts:53-57`). Saying the persona is the carrier does not make the immediately following imperative a mere pointer; it restates the operational no-silent-edit/announcement rule on a second model-facing surface. |
| MH-5 | Durable quick-pinned facts pipeline, person/project routing, reinforcement, trailer, skip+warn | **DELIVERED** | Queue publication is durable and cursor-backed (`packages/memory-core/src/facts/queue.ts:56-102,104-126`). The extractor category is a hard `QUICK_CATEGORY = "quick"`; unavailable resolution warns and skips (`packages/omo-senpi/src/components/memory/facts-runner.ts:25-26,61-70`). Project facts route to `notes/facts/<YYYY-MM>.md`; person facts route to observations only when people is enabled (`packages/memory-core/src/facts/person-routing.ts:65-114,117-134`). Exact textual reinforcement increments `n` rather than duplicating (`person-routing.ts:137-140,154-188`). Commits carry `Generated-By: facts-extractor` and `Omo-Facts-Batch` (`packages/memory-core/src/facts/extraction.ts:98-120`). |
| MH-6 | <=1500ms shutdown drain; quit-only launches; non-quit flush+enqueue; session-start relaunch | **DELIVERED** | The budget is exactly 1500 ms (`packages/omo-senpi/src/components/memory/shutdown-drain.ts:10-11,54-57`). Drain ordering is journal flush then final-delta enqueue for every reason; only quit continues to skills flush, facts launch, and evaluators (`shutdown-drain.ts:124-135`). The component awaits the drain before releasing the session (`packages/omo-senpi/src/components/memory/index.ts:128-140`). Bind-time reservation reconciliation and facts reconciliation are wired (`packages/omo-senpi/src/components/memory/wiring.ts:151-175`). The stronger no-new-work-after-abort contract is separately failed at MN-9. |
| MH-7 | Full dream command, selector, staging, automatic/manual gates, skill audit, reconciliation | **DELIVERED** | `/dream` implements auto/recent/conversation/external-transcript selection, focus, and repo-confined target documents (`packages/omo-senpi/src/components/memory/commands/dream.ts:20-110`). External staging deduplicates `source_message_id` (`packages/omo-senpi/src/components/memory/commands/dream-staging.ts:66-84`). The selector formula is exactly `50*searchHits + unreflectedSteps + recency + sourceCount + sizeFit + isCurrent - penalty` (`packages/omo-senpi/src/components/memory/dream-scoring.ts:36-59`). Automatic origins enforce enabled, spacing, shutdown, and the exact 8192-byte floor while manual bypasses them (`packages/omo-senpi/src/components/memory/dream-trigger-gates.ts:7-8,55-76`); idle, shutdown, and manual call sites are wired (`packages/omo-senpi/src/components/memory/dream-trigger.ts:123-175`). The dream persona reports unused >=90-day skills as candidates, fixes contradicted skills in place, and chooses none when unsure (`packages/memory-core/src/reflection/assets/dream-persona.md:58-76`). Session-start reconciliation scans unfinished shared reflection/dream runs (`packages/omo-senpi/src/components/memory/worker/run-reconciliation.ts:45-65`; bind call at `wiring.ts:151-159`). |
| MH-8 | People layouts/formats, `/people` surfaces, quick dialectic, palace graph, derived-only relationships | **DELIVERED** | Card prefixes, observation sections/fields, diagnostics, limits, and serialization are implemented (`packages/memory-core/src/people/format.ts:19-74,85-219,225-256`). `/people`, detail, and `--ask` are wired and gated by `people.enabled` (`packages/omo-senpi/src/components/memory/commands/people.ts:74-119`). Ask uses pinned quick, no tools, and an explicit abstention register (`packages/omo-senpi/src/components/memory/commands/people-ask.ts:16-20,45-56,75-99`). Palace returns no panel when disabled and derives every edge from card `RELATIONSHIP:` entries on each read (`packages/omo-senpi/src/components/memory/palace/people.ts:1-3,55-117`). |
| MH-9 | Documentation lands feature-local; todo 20 only audits, never authors first-pass docs | **DELIVERED** | Final documentation is present (`docs/reference/configuration.md:602-680`; `packages/omo-senpi/src/components/memory/AGENTS.md:38-71`). More importantly, compliance must be judged against the plan's explicit execution carve-out: when todo 1 had already landed without the new surface, a required same-branch backfill before the todo-20 wave was authorized (`.omo/plans/memory-v2-active-learning.md:171`). Commit history shows `82c967666 docs(memory-config): document the memory configuration surface` is an ancestor of and precedes `94ca3419f docs(memory): memory v2 configuration and divergence updates`; therefore todo 20 was not the first documentation pass. This matches the plan as written even though the first feature commit itself lacked docs. |

## Must-NOT-have rows

| Row | Guardrail | Verdict | Current evidence |
|---|---|---|---|
| MN-1 | No vector DB, embeddings, or new persisted search index; search remains FTS-lite | **DELIVERED** | Search explicitly performs a full transcript scan with “no index” (`packages/memory-core/src/search/engine.ts:1-5,48-81`). No vector/embedding/database dependency or persisted search-index artifact appears in `origin/dev..HEAD`; the divergence remains text-only (`packages/omo-senpi/src/components/memory/AGENTS.md:42-45`). |
| MN-2 | No facts model knob or category escalation | **DELIVERED** | Facts schema exposes only `enabled` and `debounce_settles` and is strict (`packages/omo-config-core/src/schema/memory.ts:43-50,115-118`). Runtime resolves only hard-coded quick and warns/skips when unavailable (`packages/omo-senpi/src/components/memory/facts-runner.ts:25-26,61-70`). |
| MN-3 | No new registry tools; `tool_exposure` remains direct by default; search opt-in | **DELIVERED** | The schema default remains `direct` (`packages/omo-config-core/src/schema/memory.ts:161-166`). Search/MCP registration occurs only for explicit `exposure === "search"`; otherwise the existing two memory tools register directly (`packages/omo-senpi/src/components/memory/tools.ts:85-125`). Dream and people are commands, not new registry tools. |
| MN-4 | No cron/service scheduler or scheduler-like timer beyond the idle timeout | **DELIVERED** | No cron, launchd/systemd unit, plist, or service artifact appears in the feature range. The automatic dream scheduler is the single idle `setTimeout` (`packages/omo-senpi/src/components/memory/dream-trigger.ts:37-53,123-129`); the other timers in this source area are bounded deadlines, retries, lock waits, or debounce mechanics rather than wall-clock schedulers. |
| MN-5 | No directional observer/observed multi-collection machinery | **DELIVERED** | No observer/observed model exists in shipped memory source. Facts payload/run ownership is identity-scoped (`packages/omo-senpi/src/components/memory/facts-runner.ts:72-93`), with no second observer dimension or collection fan-out. |
| MN-6 | No automatic contradiction resolution; no induction card rewrites; no Hermes quota; unsure => none | **DELIVERED** | Both source and packaged dream personas now preserve conflicting versions, mark both, and report for a human decision (`packages/memory-core/src/reflection/assets/dream-persona.md:54-56`; `packages/omo-senpi/plugin/extensions/dream-persona.md:54-56`). People contradictions stay open and induction never writes cards (`packages/memory-core/src/reflection/assets/dream-persona.md:82-90,102-103`; packaged copy at the same lines). Skill uncertainty chooses none (`dream-persona.md:65-76`). No Hermes one-update quota appears. |
| MN-7 | No raw `session.prompt` injection; nudge only in compiled block | **DELIVERED** | Nudge is rendered only in compiled `<memory_metadata>` (`packages/memory-core/src/compile/compile.ts:122-136`) and composed through `before_agent_start` system-prompt replacement (`packages/omo-senpi/src/components/memory/prompt.ts:41-72`). No `session.prompt` route exists in shipped memory source. |
| MN-8 | No clobbering existing repos; fresh-only seeds; v1 persona advisory only | **DELIVERED** | Seed initialization delegates to the repo's existing-HEAD no-op (`packages/memory-core/src/seeds/seeds.ts:75-92`). V1 persona detection is doctor-only and advisory (`packages/omo-senpi/src/components/memory/commands/doctor-checks.ts:83-103`). |
| MN-9 | Achievable abort contract: after 1500ms no new feature work starts; signal carried/rechecked before every operation; non-quit is flush+enqueue | **VIOLATED-PRESENT** | Several boundary checks were added and are correct in isolation: `enqueueSettled` rechecks after journal reading before `queue.enqueue` (`packages/omo-senpi/src/components/memory/facts-wiring.ts:70-81`), the skills loop checks before each tracker (`packages/omo-senpi/src/components/memory/wiring.ts:57-61`), facts checks before `launchPending` (`packages/omo-senpi/src/components/memory/facts-wiring.ts:113-124`), dream rechecks after reservation and before spawn (`packages/omo-senpi/src/components/memory/dream-trigger-fire.ts:46-63`), and journal checks immediately before `mkdir` (`packages/memory-core/src/journal/store.ts:214-217`). However the signal is still not carried to the composite operations these calls start. `FactsQueue.enqueue` accepts no signal and, after asynchronous lock/setup work, can start publication writes and watermark writes without any abort recheck (`packages/memory-core/src/facts/queue.ts:62-101,133-138,167-172,204-249`). `SkillsUsageTracker.flush` accepts no signal and can acquire its lock, create a directory, and write the ledger after the outer check (`packages/omo-senpi/src/components/memory/skills-usage.ts:182-194,205-229`). `FactsExtractorRunner.launchPending` accepts no signal and performs reconciliation, queue reads, run reservation, payload preparation, and finally child spawn after multiple awaits (`packages/omo-senpi/src/components/memory/facts-runner.ts:44-53,61-123`). Thus abort can occur after the outer call began but before a later lock acquisition/write/spawn starts, violating the plan's explicit atomic-operation boundary (`.omo/plans/memory-v2-active-learning.md:183`). Non-quit ordering itself is correct (`packages/omo-senpi/src/components/memory/shutdown-drain.ts:124-129`). |
| MN-10 | `/dream --to` accepts only memory-repo-relative paths and rejects escapes | **DELIVERED** | Absolute/drive paths are rejected, `validateMemoryPath` rejects traversal, and the normalized result must remain an in-repo `.md` path (`packages/omo-senpi/src/components/memory/commands/dream.ts:96-110`). |
| MN-11 | Optional W6 native `session_idle` work must not block W1-W5 | **DELIVERED** | Dream uses the in-extension idle timer and explicitly has no native `session_idle` listener (`packages/omo-senpi/src/components/memory/dream-trigger.ts:1-11,43-53,149-175`). Reflection, facts, dream, people, soul, and documentation are all present independently. |

## Scope check

No feature-authored source area outside the memory-v2 plan was found. The evidence rerun does not modify source. The only pre-existing dirty source file, `packages/omo-senpi/plugin/extensions/omo.js`, was not staged or altered by this audit.

## Failing rows

- **MH-4** - the memory-discipline skill still independently restates the persona's no-silent-edit/announcement rule.
- **MN-9** - abort is rechecked at outer call boundaries but is not propagated through composite queue, tracker, and extractor operations, so later lock/write/spawn work can still start after abort.

**F1: FAIL**
