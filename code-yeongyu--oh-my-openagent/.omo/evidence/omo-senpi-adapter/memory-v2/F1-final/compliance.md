# F1 Final Plan Compliance Audit - memory-v2

**Result: FAIL**

Audit basis:

- Binding checklist: `/tmp/memv2/musthave.md`
- Full plan: `.omo/plans/memory-v2-active-learning.md`
- Current source HEAD before this evidence commit: `16b65eccaa8d04f44cb52aff9df1057f21170a99`
- Prior audits: `.omo/evidence/omo-senpi-adapter/memory-v2/F1/compliance.md` and `.omo/evidence/omo-senpi-adapter/memory-v2/F1-rerun/compliance.md`
- Residual-remediation commit inspected: `a80e5da06`
- Later current-tree changes accounted for: dev-sync merge `cda4e1f00`, deadline clock fix `db528ec10`, and bundle rebuild `16b65ecca`
- Audit method: current static source, generated asset, packaged asset, commit-order, and focused-test inspection. Source was not modified.
- Pre-existing staged file `packages/omo-senpi/plugin/extensions/facts-persona.md` was not altered or included in this evidence commit.

Verdict vocabulary follows the task: **DELIVERED**, **MISSING** for an unmet Must-have, and **VIOLATED-PRESENT** for a forbidden Must-NOT behavior that remains present.

## Must-have rows

| Row | Requirement | Verdict | Current evidence |
|---|---|---|---|
| MH-1 | Exact config defaults; all feature kill switches wired end-to-end; regenerated schema | **DELIVERED** | Defaults are exact in the config schema (`packages/omo-config-core/src/schema/memory.ts:7-19,38-80,161-190`) and present in the generated schema (`assets/omo.schema.json:1225-1460`). An absent root resolves through the schema default tree (`packages/omo-senpi/src/components/memory/identity-runtime.ts:25-30`). Nudge and soul resolve enabled/edit-notice settings (`packages/omo-senpi/src/components/memory/wiring.ts:38-55`), facts resolves its enablement/debounce (`packages/omo-senpi/src/components/memory/wiring-runtime.ts:73-85`), automatic reflection carries resolved `enabled` (`wiring-runtime.ts:107-127`) and the trigger short-circuits automatic work when false (`packages/omo-senpi/src/components/memory/trigger-wiring.ts:99-116`), dream settings are resolved (`wiring.ts:92-96`) and `idleMinutes <= 0` prevents timer arming (`packages/omo-senpi/src/components/memory/dream-trigger.ts:122-128`), while people and command settings resolve through the same defaults (`wiring.ts:113-125`). |
| MH-2 | Reflection step-count default 25; explicit `step_count:0` disables | **DELIVERED** | The schema accepts nonnegative values and defaults to 25 (`packages/omo-config-core/src/schema/memory.ts:7-14,167-174`; generated copy at `assets/omo.schema.json:1233-1270`). The reflection machine requires `threshold > 0`, so zero disables the step trigger (`packages/memory-core/src/reflection/machine.ts:122-130`). |
| MH-3 | Soul v2 seed, optional identity, `<self>` v2, template bump, three notice surfaces | **DELIVERED** | Persona v2 invites self-evolution, documents optional identity, and carries the announcement instruction (`packages/memory-core/src/seeds/default-memory.ts:27-47`). Optional `system/identity.md` is read and rendered beside persona inside `<self>`, while both are excluded from generic system rendering (`packages/memory-core/src/compile/compile.ts:9-10,38-49,81-115`). Template version is v2 (`packages/omo-senpi/src/components/memory/prompt.ts:11`). Soul tool results append the unconditional directive (`packages/memory-core/src/soul/paths.ts:12-15`; `packages/memory-core/src/tools/memory.ts:85-96`; `packages/memory-core/src/tools/memory-apply-patch.ts:80-92`), the visible entry is provided by soul-notice wiring (`packages/omo-senpi/src/components/memory/soul-notice.ts:19,46-64`), and reflection-delta metadata renders in the compiled block (`packages/memory-core/src/compile/compile.ts:122-136`). |
| MH-4 | Nudge line + REMINDER v2 + memory-discipline skill; no duplicated rule across surfaces | **MISSING** | The REMINDER and nudge metadata are present (`packages/memory-core/src/compile/compile.ts:11-14,122-136`), and the skill is seeded (`packages/memory-core/src/seeds/memory-discipline.ts:18-24`; `packages/memory-core/src/seeds/seeds.ts:55-72`). The persona is the intended single carrier: “If you change this file, tell the user” (`packages/memory-core/src/seeds/default-memory.ts:45-47`). However the skill still follows its neutral single-carrier pointer with an independent imperative and rationale: “Never edit them silently: a silent edit to your own persona hides drift...” (`packages/memory-core/src/seeds/memory-discipline.ts:53-57`). That is the same no-silent-edit/user-awareness rule in different wording, so the skill does restate it. The current file also contains a duplicated `## Soul rules` heading at lines 53 and 55, confirming the intended removal did not land cleanly. |
| MH-5 | Durable quick-pinned facts pipeline, person/project routing, reinforcement, trailer, skip+warn | **DELIVERED** | Queue entries publish durably and are cursor-backed (`packages/memory-core/src/facts/queue.ts:56-105,137-175,209-260`). The extractor is hard-pinned to `QUICK_CATEGORY = "quick"` and warns/skips when unavailable (`packages/omo-senpi/src/components/memory/facts-runner.ts:25,70-74`). Project facts route to `notes/facts/<YYYY-MM>.md`, while person facts route to explicit observations only when people is enabled (`packages/memory-core/src/facts/person-routing.ts:65-114,117-134`). Textual reinforcement increments `n` instead of duplicating (`person-routing.ts:137-188`). Commits carry `Generated-By: facts-extractor` and `Omo-Facts-Batch` (`packages/memory-core/src/facts/extraction.ts:108-120`). |
| MH-6 | <=1500ms shutdown drain; quit-only launches; non-quit flush+enqueue; session-start relaunch | **DELIVERED** | The budget is exactly 1500 ms (`packages/omo-senpi/src/components/memory/shutdown-drain.ts:10-11,54-57`). Drain order is journal flush then final-delta enqueue for every reason; only quit continues to skills flush, facts launch, and evaluators (`shutdown-drain.ts:124-135`). The component awaits the drain before releasing its session (`packages/omo-senpi/src/components/memory/index.ts:128-140`). Bind-time reflection/run reconciliation and facts extractor reconciliation remain wired (`packages/omo-senpi/src/components/memory/wiring.ts:151-159`). The stronger operation-boundary contract is separately failed at MN-9. |
| MH-7 | Full dream command, selector, staging, automatic/manual gates, skill audit, reconciliation | **DELIVERED** | `/dream` exposes auto/recent/conversation/external transcript modes, focus, and a memory-repo target (`packages/omo-senpi/src/components/memory/commands/dream.ts:1-110`). External staging deduplicates by `source_message_id` (`packages/omo-senpi/src/components/memory/commands/dream-staging.ts:75-84`). The selector implements `50*searchHits + unreflectedSteps + recency + sourceCount + sizeFit + isCurrent - penalty` (`packages/omo-senpi/src/components/memory/dream-scoring.ts:36-59`). Automatic gates include enabled, spacing, and the exact 8192-byte floor while manual bypasses them (`packages/omo-senpi/src/components/memory/dream-trigger-gates.ts:7-8,55-76`); idle, shutdown, and manual call sites remain present (`packages/omo-senpi/src/components/memory/dream-trigger.ts:122-175`). The dream persona reports unused >=90-day skills as candidates, fixes contradicted skills in place, and chooses none when unsure (`packages/memory-core/src/reflection/assets/dream-persona.md:58-76`). Session-start reconciliation remains wired through the shared run store (`packages/omo-senpi/src/components/memory/worker/run-reconciliation.ts:45-65`; `wiring.ts:151-159`). |
| MH-8 | People layouts/formats, `/people` surfaces, quick dialectic, palace graph, derived-only relationships | **DELIVERED** | Card prefixes, observation sections, limits, diagnostics, and serialization remain implemented (`packages/memory-core/src/people/format.ts:19-74,85-219,225-256`). `/people`, detail, and `--ask` are registered and honor the people gate (`packages/omo-senpi/src/components/memory/commands/people.ts:74-119`). Ask is quick-pinned, tool-free, and has an explicit abstention register (`packages/omo-senpi/src/components/memory/commands/people-ask.ts:16-20,45-56,75-99`). Palace returns no panel when disabled and derives edges on read only from card `RELATIONSHIP:` entries (`packages/omo-senpi/src/components/memory/palace/people.ts:55-117`). |
| MH-9 | Documentation lands feature-local; todo 20 only audits, never authors first-pass docs | **DELIVERED** | Current docs cover the memory configuration and divergences (`docs/reference/configuration.md:602-694`; `packages/omo-senpi/src/components/memory/AGENTS.md:38-71`). The plan's execution carve-out explicitly allows a same-branch backfill before the todo-20 wave when todo 1 had already landed without the surface (`.omo/plans/memory-v2-active-learning.md:171`). Commit `82c967666` (`docs(memory-config): document the memory configuration surface`) is an ancestor of and precedes `94ca3419f` (`docs(memory): memory v2 configuration and divergence updates`), so todo 20 was not the first documentation pass. |

## Must-NOT-have rows

| Row | Guardrail | Verdict | Current evidence |
|---|---|---|---|
| MN-1 | No vector DB, embeddings, or new persisted search index; search remains FTS-lite | **DELIVERED** | Search remains a full transcript scan explicitly described as “no index” (`packages/memory-core/src/search/engine.ts:1-5,48-81`). Current memory source contains no embedding/vector-database implementation or persisted `.db`/`.sqlite` artifact; `index.ts` files are TypeScript export barrels. The declared divergence remains text-only (`packages/omo-senpi/src/components/memory/AGENTS.md:42-45`). |
| MN-2 | No facts model knob or category escalation | **DELIVERED** | Facts config exposes only `enabled` and `debounce_settles` (`packages/omo-config-core/src/schema/memory.ts:43-50,115-118`). Runtime resolves only hard-coded quick and warns/skips when unavailable (`packages/omo-senpi/src/components/memory/facts-runner.ts:25,70-74`). |
| MN-3 | No new registry tools; `tool_exposure` remains direct by default; search opt-in | **DELIVERED** | `tool_exposure` still defaults to `direct` (`packages/omo-config-core/src/schema/memory.ts:161-166`). MCP/search registration occurs only for explicit `exposure === "search"`; otherwise the existing memory tools register directly (`packages/omo-senpi/src/components/memory/tools.ts:85-125`). Dream and people remain commands rather than registry tools. |
| MN-4 | No cron/service scheduler or scheduler-like timer beyond the idle timeout | **DELIVERED** | No cron, launchd/systemd unit, plist, or service artifact is present in current memory source. The automatic dream scheduler is the single idle `setTimeout` (`packages/omo-senpi/src/components/memory/dream-trigger.ts:37-53,122-128`); other source timers are bounded operation deadlines, lock retries, liveness checks, or debounce mechanics, not wall-clock schedulers. |
| MN-5 | No directional observer/observed multi-collection machinery | **DELIVERED** | Current facts payload and run paths are identity-scoped (`packages/omo-senpi/src/components/memory/facts-runner.ts:76-93,241-253`; `packages/memory-core/src/facts/schema.ts:23-25,81-90`). No observer/observed pair, second collection dimension, or directional fan-out exists. |
| MN-6 | No automatic contradiction resolution; no induction card rewrites; no Hermes quota; unsure => none | **DELIVERED** | Source and packaged dream personas say never pick a winner, retain both versions, and report the conflict (`packages/memory-core/src/reflection/assets/dream-persona.md:54-56`; `packages/omo-senpi/plugin/extensions/dream-persona.md:54-56`). People contradictions remain open and induction never writes cards (`packages/memory-core/src/reflection/assets/dream-persona.md:82-90,102-103`), while uncertainty chooses none (`dream-persona.md:65-76`). No Hermes one-update quota is present. |
| MN-7 | No raw `session.prompt` injection; nudge only in compiled block | **DELIVERED** | Nudge renders only in compiled `<memory_metadata>` (`packages/memory-core/src/compile/compile.ts:122-136`) and is composed through the v2 before-agent-start system-prompt path (`packages/omo-senpi/src/components/memory/prompt.ts:41-72`). No `session.prompt` route exists in current memory source. |
| MN-8 | No clobbering existing repos; fresh-only seeds; v1 persona advisory only | **DELIVERED** | Seed initialization delegates to the repo initializer whose existing-HEAD behavior is a no-op (`packages/memory-core/src/seeds/seeds.ts:1-19,75-92`). V1 persona detection is doctor-only and advisory (`packages/memory-core/src/seeds/default-memory.ts:18-24`; `packages/omo-senpi/src/components/memory/commands/doctor-checks.ts:83-103`). |
| MN-9 | Achievable abort contract: after 1500ms no new feature work starts; signal carried/rechecked before every operation; non-quit is flush+enqueue | **VIOLATED-PRESENT** | Signal threading through shutdown call paths is present: wiring passes the signal to tracker flush and facts operations (`packages/omo-senpi/src/components/memory/wiring.ts:57-87`), and facts wiring passes it into queue enqueue and extractor launch (`packages/omo-senpi/src/components/memory/facts-wiring.ts:69-85,113-124`). Several interior checks also exist, but they do not cover all required boundaries. **FactsQueue:** `enqueue` checks before calling `publish` and before `advanceEnqueued` (`packages/memory-core/src/facts/queue.ts:99-103`), but `locked` performs directory creation and lock acquisition before the callback can check the signal (`queue.ts:137-142`); `publish` can start its rename after an abort occurring during the preceding write (`queue.ts:171-175`); and `advanceEnqueued` awaits a cursor read before beginning `writeCursor` without another signal check (`queue.ts:209-218`). **SkillsUsageTracker:** `flushBatch` checks before `createLockRecord` and before `withLock` (`packages/omo-senpi/src/components/memory/skills-usage.ts:206-215`), but after acquiring the lock its callback awaits the ledger read and then starts `mkdir` and `writeLedgerAtomic` with no live abort recheck (`skills-usage.ts:215-227`). This directly misses the requested “before ... write” boundary. **FactsExtractorRunner:** checks occur at function entry, after queue listing, and before child spawn (`packages/omo-senpi/src/components/memory/facts-runner.ts:62-67,108-110`), but after possible asynchronous repo initialization there is no check immediately before `reserveFactsRunDir` (`facts-runner.ts:76-88`). This directly misses the requested “before reservation” boundary. The three focused tests exist (`packages/memory-core/src/facts/queue.test.ts:178-195`; `packages/omo-senpi/src/components/memory/skills-usage-wiring.test.ts:101-126`; `packages/omo-senpi/src/components/memory/facts-runner-abort.test.ts:8-24`) and pass, but each supplies a signal that is already aborted before the composite starts; none flips the signal at the interior await/boundary, so they do not prove the binding no-new-operation-after-abort contract. Non-quit flush-plus-enqueue ordering itself remains correct (`packages/omo-senpi/src/components/memory/shutdown-drain.ts:124-129`). |
| MN-10 | `/dream --to` accepts only memory-repo-relative paths and rejects escapes | **DELIVERED** | Absolute and drive paths are rejected, `validateMemoryPath` rejects traversal, and the normalized in-repo result must end in `.md` (`packages/omo-senpi/src/components/memory/commands/dream.ts:96-110`). |
| MN-11 | Optional W6 native `session_idle` work must not block W1-W5 | **DELIVERED** | Dream continues to use the in-extension idle timer and explicitly registers no native `session_idle` listener (`packages/omo-senpi/src/components/memory/dream-trigger.ts:1-11,37-53,149-175`). Reflection, facts, dream, people, soul, and docs are all present independently. |

## Focused test corroboration

Only the three permitted files were run:

```text
bun test packages/memory-core/src/facts/queue.test.ts \
  packages/omo-senpi/src/components/memory/skills-usage-wiring.test.ts \
  packages/omo-senpi/src/components/memory/facts-runner-abort.test.ts
```

Result: **12 pass, 0 fail, 32 expect() calls**. These tests establish pre-aborted entry behavior, but not signal transitions at the uncovered interior boundaries described under MN-9.

## Scope check

No newly introduced source surface outside the memory-v2 plan was found in the current tree. The dev-sync merge and deadline clock fix changed current paths/line numbers but do not repair MH-4 or the missing MN-9 interior checks. The audit wrote only this evidence file; the pre-existing staged packaged asset was left untouched.

## Failing rows

- **MH-4** - `memory-discipline.ts` still restates the persona's announcement/no-silent-edit rule after its neutral pointer.
- **MN-9** - the signal is threaded, but current composites still permit new lock/write/reservation work to start after an abort at uncovered interior boundaries; the focused tests only exercise already-aborted entry.

**F1: FAIL**
