PLAN/CODE DISCREPANCIES FOUND DURING TODO 9

1. IC-8 supervisor bootstrap self-enforcement was absent in the landed todo-25 implementation when todo 9 began. `runChildBootstrap()` read the release byte and spawned the model child but armed no timers against `hardDeadlineAt` or `hardDeadlineAt + terminationGraceMs`; the abrupt-supervisor integration test only proved the recorded child group could be manually killed. Concurrent todo-13 work began modifying the same supervisor during this task, so todo 9 did not take ownership of that file.

2. Todo 9's QA line requests a "senpi -p 4-turn scripted session", while the authoritative verification strategy in the same plan states print mode is single-shot and exits after one prompt. Four accepted turns require a persistent TUI/RPC session; `-p` cannot provide them. Todo 9 therefore proves the four-settle threshold with a deterministic component test and attempted a separate isolated `senpi -p` recovery exercise. The source-loaded live exercise did not observe a facts run directory before its bounded watcher expired, and its failed output remains in `senpi-run.txt`; no passing live-Senpi claim is made from that attempt.
