# F1 Plan Compliance Audit - memory-v2

**Result: FAIL**

Audit basis:

- Binding checklist: `/tmp/memv2/musthave.md`
- Full plan: `.omo/plans/memory-v2-active-learning.md`
- Shipped range: `origin/dev..HEAD` at pre-evidence HEAD `d97ed50c8`
- Audit method: static source/commit inspection. Source files were not modified.

Verdict vocabulary follows the task: **DELIVERED**, **MISSING** for an unmet Must-have, and **VIOLATED-PRESENT** for a forbidden Must-NOT behavior that shipped.

## Must-have rows

| Row | Requirement | Verdict | Evidence |
|---|---|---|---|
| MH-1 | Exact config defaults; all feature kill switches wired end-to-end; regenerated schema | **MISSING** | Schema/default values and generated schema exist (`packages/omo-config-core/src/schema/memory.ts:7-19,38-80,161-190`; `assets/omo.schema.json:1233-1460`). The adapter fallback also contains the v2 tree (`packages/omo-senpi/src/components/memory/index.ts:24-52,174-176`). End-to-end wiring is incomplete: automatic reflection sessions omit the resolved `enabled` value (`packages/omo-senpi/src/components/memory/wiring.ts:235-252`) while the machine config has no enabled field (`packages/memory-core/src/reflection/machine.ts:14-17,54-58`), so `reflection.enabled:false` does not gate automatic reservation. The no-top-level-`memory` runtime path also bypasses the adapter fallback: nudge resolves to disabled (`wiring.ts:90-99`), soul notice resolves false (`wiring.ts:102-109`), facts resolves false (`wiring.ts:201-212`), palace people settings resolve absent (`wiring.ts:304-311`), and command settings throw (`wiring.ts:314-317`) when raw `config.memory` is absent. Dream does use its own fallback (`wiring.ts:141-148`) and `idle_minutes <= 0` prevents timer arming (`dream-trigger.ts:209-214`). |
| MH-2 | Reflection step-count default 25; explicit `step_count:0` disables | **DELIVERED** | Default is exactly 25 in schema and adapter/runtime fallbacks (`packages/omo-config-core/src/schema/memory.ts:7-14,167-174`; `packages/omo-senpi/src/components/memory/index.ts:28-35`; `identity-runtime.ts:31-38`). Zero is accepted by the schema (`memory.ts:8`) and the state machine requires `threshold > 0` (`packages/memory-core/src/reflection/machine.ts:122-130,252-255`). |
| MH-3 | Soul v2 seed, optional identity, `<self>` v2, template bump, three notice surfaces | **DELIVERED** | Persona seed contains self-evolution and the user-announcement instruction (`packages/memory-core/src/seeds/default-memory.ts:26-47`). Optional `system/identity.md` is rendered after persona inside `<self>` and excluded from generic system memory (`packages/memory-core/src/compile/compile.ts:9-10,39-49,81-115`). Template is `omo-senpi:before_agent_start:v2` (`packages/omo-senpi/src/components/memory/prompt.ts:11`). Soul tool-result discipline is unconditional for soul paths (`packages/memory-core/src/tools/memory.ts:85-96`; `memory-apply-patch.ts:80-92`), visible notice is gated by `edit_notice` (`packages/omo-senpi/src/components/memory/soul-notice.ts:46-64`), and reflection delta metadata is rendered (`packages/memory-core/src/compile/compile.ts:122-136`). |
| MH-4 | Nudge line + REMINDER v2 + memory-discipline skill; no duplicated rule across surfaces | **MISSING** | Nudge metadata and exact REMINDER v2 exist (`packages/memory-core/src/compile/compile.ts:11-14,122-136`); the seeded skill exists (`packages/memory-core/src/seeds/memory-discipline.ts:18-24`; `seeds/seeds.ts:55-72`). However the soul-announcement operational rule is duplicated: persona says to tell the user after changing the soul (`default-memory.ts:47`) and the skill independently says to tell the user whenever changing a system self-model file (`memory-discipline.ts:53-55`). This violates the row's “each rule has exactly one point of use” condition. |
| MH-5 | Durable quick-pinned facts pipeline, person/project routing, reinforcement, trailer, skip+warn | **DELIVERED** | Durable queue and cursor implementation is in `packages/memory-core/src/facts/queue.ts:1-260`. Extractor category is a hard constant `QUICK_CATEGORY = "quick"`; unavailable resolution warns and skips without fallback (`packages/omo-senpi/src/components/memory/facts-runner.ts:39-43,112-121`). Routing sends project facts to `notes/facts/<YYYY-MM>.md`, person facts to observations when people is enabled (`packages/memory-core/src/facts/person-routing.ts:65-114,117-134`), and exact textual reinforcement increments `n` (`person-routing.ts:137-140,154-185`). Commit trailers include `Generated-By: facts-extractor` and `Omo-Facts-Batch` (`packages/memory-core/src/facts/extraction.ts:108-120`). |
| MH-6 | <=1500ms shutdown drain; quit-only launches; non-quit flush+enqueue; session-start relaunch | **DELIVERED** | Pinned constant is exactly 1500 (`packages/omo-senpi/src/components/memory/shutdown-drain.ts:10-11,54-57`). Drain order is journal flush, final-delta enqueue, then quit-only skills flush/facts launch/evaluators (`shutdown-drain.ts:124-135`); index awaits it before releasing the session (`packages/omo-senpi/src/components/memory/index.ts:157-168`). Bind-time reconciliation runs reservation reconcile and facts extractor reconcile (`packages/omo-senpi/src/components/memory/wiring.ts:425-433`). The stronger no-new-work-after-abort contract is separately failed at MN-9. |
| MH-7 | Full dream command, selector, staging, automatic/manual gates, skill audit, reconciliation | **DELIVERED** | Command surface and repo-confined `--to` are implemented (`packages/omo-senpi/src/components/memory/commands/dream.ts:1-25,33-84,96-110`). Selector formula is exactly `50*searchHits + unreflectedSteps + recency + sourceCount + sizeFit + isCurrent - penalty` (`packages/omo-senpi/src/components/memory/dream-selector.ts:72-95`). External staging dedupes by `source_message_id` (`commands/dream-staging.ts:75-84`). Automatic gates use enabled, min-hours, and exact 8192-byte constant while manual bypasses them (`dream-trigger.ts:30-31,78-100`); timer/shutdown/manual call sites are present (`dream-trigger.ts:209-233,290-317`). Skill audit uses the ledger, reports unused >=90d as CANDIDATE, fixes contradicted skills in place, and chooses none when unsure (`packages/memory-core/src/reflection/assets/dream-persona.md:58-76`). Session-start reservation-run reconciliation covers dream because dream shares reflection runs (`packages/omo-senpi/src/components/memory/worker/run-reconciliation.ts:45-65`; `wiring.ts:425-433`). |
| MH-8 | People layouts/formats, `/people` surfaces, quick dialectic, palace graph, derived-only relationships | **DELIVERED** | Formats and limits are implemented in `packages/memory-core/src/people/format.ts:19-74,85-219,225-257`. `/people`, detail, and `--ask` are wired (`packages/omo-senpi/src/components/memory/commands/people.ts:74-119`); ask uses pinned quick and an abstention register (`commands/people-ask.ts:16-20,45-56,75-99`). Palace graph is derived on read solely from card `RELATIONSHIP:` entries and never persisted (`packages/omo-senpi/src/components/memory/palace/people.ts:1-3,55-117`). |
| MH-9 | Documentation lands feature-local; todo 20 only audits, never authors first-pass docs | **MISSING** | The memory configuration surface was authored in one later docs commit, `82c967666 docs(memory-config): document the memory configuration surface`, after numerous feature commits; `git log --follow -- docs/reference/configuration.md` shows no feature-local docs commits. The divergence rows were added only in `94ca3419f docs(memory): memory v2 configuration and divergence updates`; `git log --follow -- packages/omo-senpi/src/components/memory/AGENTS.md` confirms that centralized update. Final docs do exist (`docs/reference/configuration.md:602-666`; `packages/omo-senpi/src/components/memory/AGENTS.md:38-71`), but the required commit locality/order did not ship. |

## Must-NOT-have rows

| Row | Guardrail | Verdict | Evidence |
|---|---|---|---|
| MN-1 | No vector DB, embeddings, or new persisted search index; search remains FTS-lite | **DELIVERED** | Search explicitly performs a full transcript scan with “no index” (`packages/memory-core/src/search/engine.ts:1-5,48-81`). No vector/embedding dependency or database/index artifact appears in `git diff origin/dev..HEAD`; newly added `facts/index.ts`, `people/index.ts`, and `soul/index.ts` are TypeScript export barrels, not data indexes. Declared divergence remains text-only (`packages/omo-senpi/src/components/memory/AGENTS.md:42-45`). |
| MN-2 | No facts model knob or category escalation | **DELIVERED** | Facts schema has only `enabled` and `debounce_settles` and is strict (`packages/omo-config-core/src/schema/memory.ts:43-50,115-118`). Runtime resolves only `QUICK_CATEGORY` and skips+warns on unavailability (`packages/omo-senpi/src/components/memory/facts-runner.ts:39,112-121`). |
| MN-3 | No new registry tools; `tool_exposure` remains direct by default; search opt-in | **DELIVERED** | Default remains `direct` in schema and adapter (`packages/omo-config-core/src/schema/memory.ts:161-166`; `packages/omo-senpi/src/components/memory/index.ts:24-28`). Search/MCP requires explicit `exposure === "search"`; otherwise existing memory tools register directly (`packages/omo-senpi/src/components/memory/tools.ts:99-123`). No new tool definition was added by this plan; dream/people are slash commands. |
| MN-4 | No cron/service scheduler or scheduler-like timer beyond the idle timeout | **DELIVERED** | No cron, launchd/systemd unit, plist, service, or timer artifact appears in the shipped diff. The automatic dream scheduler is the single idle `setTimeout` (`packages/omo-senpi/src/components/memory/dream-trigger.ts:132-148,209-214`); other source timers are bounded operation deadlines/retries/debounces, not wall-clock schedulers. |
| MN-5 | No directional observer/observed multi-collection machinery | **DELIVERED** | No observer/observed data model was added in the shipped memory diff. Facts payload and run ownership are identity-scoped (`packages/omo-senpi/src/components/memory/facts-runner.ts:64-83,123-137`), with the observer effectively fixed to the bound agent identity. |
| MN-6 | No automatic contradiction resolution; no induction card rewrites; no Hermes quota; unsure => none | **VIOLATED-PRESENT** | The dream persona explicitly instructs automatic contradiction resolution: “when files disagree, fix the stale entry at the source in favor of the latest evidence” (`packages/memory-core/src/reflection/assets/dream-persona.md:52-56`). That is forbidden by the absolute guardrail even though the later people phase correctly says people contradictions stay open and induction never writes cards (`dream-persona.md:82-90,102-103`). No Hermes one-update quota shipped, and uncertainty correctly chooses none (`dream-persona.md:65-76`). |
| MN-7 | No raw `session.prompt` injection; nudge only in compiled block | **DELIVERED** | Nudge is emitted only from compiled `<memory_metadata>` (`packages/memory-core/src/compile/compile.ts:122-136`) and injected through `before_agent_start` system-prompt composition (`packages/omo-senpi/src/components/memory/prompt.ts:41-72`). No `session.prompt` route exists in shipped memory source. |
| MN-8 | No clobbering existing repos; fresh-only seeds; v1 persona advisory only | **DELIVERED** | Seed adapter delegates to repo init and documents existing-HEAD no-op (`packages/memory-core/src/seeds/seeds.ts:1-19,75-92`). V1 persona hash is advisory and doctor-only (`packages/memory-core/src/seeds/default-memory.ts:18-24`; `packages/omo-senpi/src/components/memory/commands/doctor-checks.ts:84-99`). |
| MN-9 | Achievable abort contract: after 1500ms no new feature work starts; signal carried/rechecked before every operation; non-quit is flush+enqueue | **VIOLATED-PRESENT** | The top-level drain races steps and aborts at return (`packages/omo-senpi/src/components/memory/shutdown-drain.ts:84-115,124-140`), and non-quit correctly performs flush+enqueue (`shutdown-drain.ts:124-127`). But the signal is not carried end-to-end: final enqueue checks once then calls non-cancellable `enqueueSettled` (`packages/omo-senpi/src/components/memory/wiring.ts:124-128`; `facts-wiring.ts:70-81`), skills flush ignores the signal inside tracker flushes (`wiring.ts:112-114,129-132`), and facts launch checks once then calls a non-signal API (`wiring.ts:133-137`; `facts-wiring.ts:113-123`). Dream shutdown checks before reservation, but can abort during `tryReserve` and still start `session.launch` afterward because there is no post-reservation signal check (`dream-trigger.ts:252-287`). Journal locking also checks before `mkdir` but does not re-check immediately before that operation (`packages/memory-core/src/journal/store.ts:214-217`). Therefore new writes/spawns can begin after abort, contrary to this row. |
| MN-10 | `/dream --to` accepts only memory-repo-relative paths and rejects escapes | **DELIVERED** | Absolute/drive paths are rejected, `validateMemoryPath` rejects traversal, and only repo-relative `.md` paths are returned (`packages/omo-senpi/src/components/memory/commands/dream.ts:96-110`). |
| MN-11 | Optional W6 native `session_idle` work must not block W1-W5 | **DELIVERED** | Dream idle behavior ships using the in-extension idle timer and explicitly registers no native `session_idle` listener (`packages/omo-senpi/src/components/memory/dream-trigger.ts:5-11,138-148,290-302`). W1-W5 features are present independently. |

## Scope check

No feature-authored source area beyond the plan was found. The broad non-memory files visible in `git diff origin/dev..HEAD` enter through upstream merge commit `43e47c610 Merge dev into feat/memory-v2-active-learning: pick up senpi pin` (second parent `56593dc78`), rather than through a memory-v2 feature commit. They are recorded here because the requested comparison range includes that merge, but they are baseline synchronization, not an added memory-v2 feature surface.

## Failing rows

- **MH-1** - default-on and kill-switch wiring is not end-to-end.
- **MH-4** - the soul-announcement rule is duplicated across persona and skill.
- **MH-9** - documentation did not land feature-local.
- **MN-6** - automatic contradiction-resolution instruction shipped.
- **MN-9** - the absolute abort/no-new-work boundary is not propagated through all drain operations.

**F1: FAIL**
