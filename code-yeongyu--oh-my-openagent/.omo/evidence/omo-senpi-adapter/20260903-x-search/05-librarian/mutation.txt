bun test v1.4.0 (34cbb9a40)

packages/senpi-task/src/agents/builtin/builtin-agents.test.ts:
(pass) builtin curated agents > #given the builtin defaults #when listing names sorted #then the 4 curated and 3 reviewer agents are present [0.95ms]
(pass) builtin curated agents > #given the builtin record #when listing keys sorted #then every builtin maps to its definition [0.10ms]
(pass) builtin curated agents > #given the curated name set #when checking membership #then it contains exactly the 4 curated names [0.10ms]
(pass) builtin curated agents > #given the reviewer name set #when checking membership #then it contains exactly the 3 reviewer names and stays disjoint from the curated set [0.13ms]
(pass) builtin curated agents > #given every builtin definition #when inspecting shape #then mode is subagent and executionMode is pinned in-process [1.76ms]
66 | 
67 | 
68 |   test("#given every curated definition #when inspecting tool rules #then the expected literal allow-true rules are present", () => {
69 |     for (const definition of CURATED_READONLY_AGENT_DEFAULTS) {
70 |       const expected = definition.name === "librarian" ? EXPECTED_LIBRARIAN_TOOL_ALLOWLIST : EXPECTED_TOOL_ALLOWLIST
71 |       expect(definition.tools).toHaveLength(expected.length)
                                    ^
error: expect(received).toHaveLength(expected)

Expected length: 10
Received length: 9

      at <anonymous> (/Volumes/mengmotaStorage/local-workspaces/omo-wt/x-search/packages/senpi-task/src/agents/builtin/builtin-agents.test.ts:71:32)
(fail) builtin curated agents > #given every curated definition #when inspecting tool rules #then the expected literal allow-true rules are present [0.28ms]
(pass) builtin curated agents > #given every reviewer definition #when inspecting tool rules #then the curated allowlist plus write is present [0.07ms]
(pass) builtin curated agents > #given the reviewer definitions #when reading their names #then they match the ulw-loop omo-senpi quality-gate identities [0.06ms]
(pass) builtin curated agents > #given every builtin definition #when inspecting descriptions #then each is non-empty with the brand tag stripped [0.07ms]

 8 pass
 1 fail
 112 expect() calls
Ran 9 tests across 1 file. [562.00ms]
mutation_exit=1

--- restored run ---
bun test v1.4.0 (34cbb9a40)
