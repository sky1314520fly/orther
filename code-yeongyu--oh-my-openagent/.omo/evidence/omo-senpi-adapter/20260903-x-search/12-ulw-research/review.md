# Todo 12 review: ulw-research X / social lane

Source: `packages/omo-senpi/skills/ulw-research/SKILL.md` (native senpi pool, shipped verbatim by `sync-skills.mjs`).
Shipped copy: `packages/omo-senpi/plugin/skills/ulw-research/SKILL.md` (regenerated by `bun run build:senpi-plugin`, see `build.txt`).

## Anchor re-verification (worktree at 130a5c516 vs plan refs at 24c909c4e)

| Plan ref | Actual before edit | Drift |
|---|---|---|
| :80 brief line | line 80 `Codebase relevant: ... Verification likely: <yes/no>` | none |
| :171-173 scaling floor table | 171 intro sentence, 173 table header, 175-179 rows | none |
| :183-187 role protocols | 183 intro, 185 Codebase, 186 Web, 187 Browsing | none |
| :359 `## Search craft` | line 359 | none |

## What changed (read-through)

1. Brief line (line 80): appended ` · X/social signal: <yes/no>` so the Phase 0 brief carries the yes/no that drives the X lane.
2. Role protocols: new bullet right after Browsing (now line 190 in both copies), text as the plan specified: tool_search probe, x-search skill rules (from_date, allowed_x_handles, latest/recent, operators, 2-3 split searches, one call per search), lane owner (`librarian` lane or category member, never curated explore), `Queries used:` provenance, web-lane reconciliation before the claim graph, and the `x_search: unavailable` fallback.
3. Scaling floor table: new `X lanes` column with `0 or 1` on every row and `(+1)` on the floor column, plus one sentence below the table pinning the arithmetic (1 when the brief says yes, 0 otherwise, so Multi-faceted with X signal = 18). Existing floors are untouched for the no case, so no existing arithmetic breaks.
4. Search craft: new bullet after the operator table listing the X operators (`since: until: from: filter:links -filter:replies lang:ko`) and the latest/recent phrasing rule.

## Checks

- `red.txt`: new assertion fails on the pre-edit shipped copy (Received: false).
- `build.txt`: `bun run build:senpi-plugin` exit 0, `synced omo-senpi skills` line present.
- `grep.txt`: all four insertions present in the shipped copy; blank-line-normalized source and shipped copy are byte-identical.
- `green.txt`: `bun test packages/omo-senpi/src/skills-sync.test.ts` 12 pass / 0 fail.
- `mutation.txt`: breaking the token in the shipped copy fails only the new assertion; restore brings 12/12 back.

## Wording notes

The one machine check is the literal token `X / social (\`x_search\``, which is the bullet's anchor and what downstream readers grep for. No prose wording is pinned. The bullet uses the plan's exact phrasing; the file's existing em-dash style was left alone outside the inserted text, and the inserted text uses colons and semicolons, not dashes.

Omitted: no tokens, auth files, or env dumps were read or written.
