# What was intentionally omitted

- No OpenCode, Codex, main-checkout, or unrelated package code was changed.
- No process-runner persona/tool-policy redesign or broad team eligibility registry was added.
- No real model API, authenticated GitHub request, or network document fetch was needed; the safe live broker probe used `curl --version`.
- No raw secret-bearing logs, user configuration, environment dump, or transcript body was retained.
- `.omo/**`, temporary QA sandboxes, dependencies, and generated test caches are not staged for the commit.
