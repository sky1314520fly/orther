Commands:
1. npm exec --yes --package=bun@1.3.12 -- bash -c \
   'node packages/omo-senpi/plugin/scripts/build-extension.mjs'
2. npm exec --yes --package=bun@1.3.12 -- bun run build:lsp-daemon
3. node packages/omo-senpi/plugin/scripts/stage-lsp-daemon-runtime.mjs
4. npm exec --yes --package=bun@1.3.12 -- bash -c \
   'node packages/omo-senpi/plugin/scripts/build-extension.mjs --check'

Observed:
- The first build regenerated all six omo-senpi extension artifacts.
- The first check exposed a missing local staged LSP manifest, so the repository's
  `build:lsp-daemon` and staging scripts prepared that prerequisite.
- The final check reported:
  `omo-senpi extension build is current: .../packages/omo-senpi/plugin/extensions/omo.js`
- Exit code 0.

Non-blocking existing dependency notice:
`npm ci` in packages/lsp-daemon reported two high-severity audit findings. No dependency
versions or lockfiles were changed by this task; the notice is unrelated to the telemetry
source change and was not suppressed.
