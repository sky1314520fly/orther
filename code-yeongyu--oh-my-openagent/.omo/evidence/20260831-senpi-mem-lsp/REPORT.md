# LSP pool fixes — RED/GREEN evidence (20260831)

Branch: `fix/senpi-mem-lsp-pool` (base `origin/dev` @ d50518d45)
Scope: `packages/lsp-core` only (+ regenerated `packages/omo-senpi/plugin/extensions/omo.js` per bundle rule).
Defects: SENPI-SUSPECT-MATRIX rows 1.1 and 1.3 (`../ulw-loop/omo-senpi-memleak-20260831/reports/`, `lsp-lifecycle.md` S1–S4).
Test runner: remote mac runner (`MAC_TEST_TIMEOUT_S=1800 bun /tmp/omo-mac-test2.mjs fix/senpi-mem-lsp-pool -- packages/lsp-core`), bun 1.4.0. No local `bun test` was run at any point.

## TDD sequence

| run | SHA | commits | result |
|---|---|---|---|
| RED | `3ea5b056e` | test(lsp-core): pin monorepo root collapse and stop/respawn pool invariants | **8 fail / 149 pass** — exactly the 9 new tests' failing cases (1 pre-existing-pass contract test: no-git fallback keeps nearest package root) |
| GREEN (1st) | `50673b9c1` | + both fix commits | 1 fail / 156 pass — harness defect (init failure applied to every generation; replacement client also failed init), fixed in `0173eb2be` |
| GREEN (final) | `0173eb2be` | + test(lsp-core): fail initialization on the first generation only | **157 pass / 0 fail** |

Raw captures: `red-run.txt`, `green-run.txt` (runner output, includes SHA header).

## RED (SHA 3ea5b056e) — the two defects, pinned

Fix 1 (matrix 1.1) — per-package roots: a monorepo fixture (`.git` at repo root, 4 nested `package.json`) currently yields 4 distinct roots and 4 clients:

```
error: expect(received).toBe(expected)
Expected: 1
Received: 4
(fail) LSP workspace root collapse in monorepos > #given a git repository containing four package.json packages ... all collapse to the repository root

expect(factoryRoots).toEqual([realpathSync(root)]);
error: expect(received).toEqual(expected)
  [ - "/tmp/.../lsp-client-wrapper-oneclient-XXXX"                          (expected: repo root once)
  + ".../packages/alpha", ".../packages/beta", ".../packages/gamma", ".../packages/delta" ]  (received: per-package)
(fail) LSP workspace root collapse in monorepos > #given four monorepo package files under one .git root ... one client serves the whole repository
```

Fix 2 (matrix 1.3) — delete-before-stop duplicate-spawn window: the replacement spawns while the old client's stop is still pending (`stop-end` missing before `spawn`), on all three eviction shapes (cap eviction, invalidation, init failure):

```
expect(log.entries.filter((e) => e.endsWith("/root-a"))).toEqual([
  "spawn:/root-a", "stop-start:/root-a", "stop-end:/root-a", "spawn:/root-a",
]);
error: expect(received).toEqual(expected)
  [ "spawn:/root-a", "stop-start:/root-a", - "stop-end:/root-a", "spawn:/root-a" ]
(fail) LspManager pending-stop tombstones > #given a slow-stopping client evicted at the resident cap ...

(fail) LspManager pending-stop tombstones > #given an invalidated client whose stop is still pending ...
(fail) LspManager pending-stop tombstones > #given a client whose initialization failed while its stop is pending ...
```

Fix 2 (matrix 1.3) — unbounded respawn: a crash-looping fake (dead on every `isAlive`) respawns per request and never surfaces a budget error:

```
expect(clients.length).toBeLessThanOrEqual(3);
Expected: <= 3
Received: 8
(fail) LspManager dead-client respawn budget > #given a crash-looping client ... respawn attempts stay within the retry budget

expect(manager.getClient(...)).rejects.toThrow(/respawn budget/)
Expected promise that rejects
Received promise that resolved: Promise { <resolved> }
(fail) LspManager dead-client respawn budget > #given the respawn budget is exhausted ... (x2: budget error + cooldown recovery)
```

## GREEN (SHA 0173eb2be)

```
SHA=0173eb2be
 157 pass
 0 fail
Ran 157 tests across 29 files. [5.06s]
```

All 149 pre-existing lsp-core tests pass unchanged (no existing test pinned per-package-when-`.git`-exists semantics; the lsp-tools-mcp nested-workspace test has no `.git` fixture and keeps passing under the fallback rule).

## Verification beyond the scoped suite

- Typecheck (repo convention): `tsgo --noEmit -p tsconfig.json` clean for `packages/lsp-core`; `tsc --noEmit` clean for `packages/lsp-daemon` and `packages/lsp-tools-mcp` (direct consumers of the changed modules).
- Bundle rule: `node packages/omo-senpi/plugin/scripts/build-extension.mjs --check` initially reported the staged lsp-daemon runtime manifest stale; after `bun run build:senpi-plugin` (bun 1.4.0) it reports current. Regenerated tracked artifact `packages/omo-senpi/plugin/extensions/omo.js` committed via `git add -f`.
- Not committed: `assets/omo.schema.json`, `assets/oh-my-opencode.schema.json`, `packages/omo-codex/scripts/install-dist/install-local.mjs` — regenerated by the same build but semantically belong to other lanes' already-merged dev changes (pre-existing artifact staleness on dev: `maxLiveDescendantsPerRoot` schema fields, beta.29→beta.30 version stamp). Left for their owners.
- Not verified here: `scripts/qa/lsp-e2e.mjs` live QA (spawns real language servers) — out of the sanctioned remote scope for this task; the unit seams cited by matrix 1.1/1.3 are covered above.
