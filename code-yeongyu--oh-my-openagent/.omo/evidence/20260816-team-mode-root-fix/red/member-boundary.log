Commands:
  bun test packages/omo-senpi/src/components/task/index.test.ts packages/senpi-task/src/runners/rpc/spawn.test.ts
  bun test packages/senpi-task/src/runners/rpc/spawn.test.ts -t 'generic child spawned by a member'

Base:
  origin/dev at 5c4c06c3967f25f470589c683940da8a8bdb2c5a
  PR B production source unchanged.

Result:
  RED (exit 1), for both intended member-boundary regressions.

Observed failure 1 — explicit member process:
  Expected registered tools: []
  Received lead/task controls:
    task
    task_cancel
    task_create
    task_get
    task_list
    task_output
    task_send
    task_update
    team_create
    team_delete

Observed failure 2 — generic descendant of a member:
  Expected `descriptor.env.SENPI_TASK_MEMBER` to be undefined.
  Received `11111111-1111-4111-8111-111111111111::alice`.

Aggregate:
  33 pass
  2 fail
  88 expectations

Positive control:
  The existing explicit-member spawn case still carries member identity, task id, team config, isolated session directory, and the member extension.

Why this is valid RED:
  The failures are assertion mismatches on the exact control-surface and inherited-identity defects.
  No syntax, import, fixture, timing, dependency, or setup failure occurred.

Setup note:
  `bun install` rebuilt generated extension bundles with local Bun as a postinstall side effect. Those unrelated generated diffs are not RED evidence and will be replaced by the final CI Bun 1.3.12 build after production changes.
