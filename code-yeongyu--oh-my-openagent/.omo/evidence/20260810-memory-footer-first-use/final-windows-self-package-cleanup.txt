Final Windows root CI cleanup

Observed CI failure:
- Authoritative run: 31383351855
- Windows root job: 93438288729
- Current Git tree contained neither failing test path.
- Bun executed stale self-package copies through package-local `node_modules`:
  - `packages/omo-opencode/src/cli/ralph-compatibility.test.ts`
  - `packages/omo-senpi/src/__tests__/registration-design-audit.test.ts`
- The stale Ralph CLI test exhausted its 30-second test budget.
- The stale registrar audit expected 72 registrations but saw the new memory `tool_result` registrar as registration 73.

RED:
- Added `script/remove-stale-self-package-tests.test.ts`.
- Initial exact Bun 1.3.12 run failed because `remove-stale-self-package-tests.ts` did not exist.

GREEN:
- The cleanup scans only `packages/*/node_modules/{oh-my-opencode,oh-my-openagent}`.
- It removes those stale self-package copies and leaves unrelated dependencies untouched.
- The root test and Senpi compatibility jobs invoke it after dependency installation and before their Bun suites.
- Focused test: 2 pass, 0 fail, 10 assertions.
- Script typecheck: exit 0.
- TypeScript no-excuse audit: no violations.
- `actionlint .github/workflows/ci.yml`: exit 0.
- Real workspace invocation: no stale self-package test copies found on macOS.

Windows proof:
- In run 31385945591, the stale Ralph and registrar failures did not recur, confirming the cleanup removed that failure mode.
- That run did not complete green: the Windows root job was cancelled, and Senpi Windows failed during fixture teardown.
- The branch was then synced with the latest `dev`; its replacement matrix must pass before PR merge.

Senpi job proof:
- Run 31392779021 exposed a hermetic Senpi typecheck failure in `senpi-compatibility (macos-latest)`.
- The stale package-local self-package copy lacked the current `@oh-my-opencode/omo-opencode/config-migration` export, causing TS2307 in `config-startup/index.ts` and its test.
- That job did not run the cleanup step, so stale self-package types remained resolvable ahead of the current workspace package.
- The workflow now runs the same cleanup before both root `bun test` and `bun test packages/omo-senpi`.
- The same run's Senpi Windows job also timed out two pre-existing Git-backed `/memfs diff` cases at 5437 ms and 5125 ms; those are outside this branch's diff and remain explicit replacement-matrix gates.
