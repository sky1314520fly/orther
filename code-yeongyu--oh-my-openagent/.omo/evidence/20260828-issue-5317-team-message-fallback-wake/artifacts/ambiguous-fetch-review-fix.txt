Ambiguous fetch review fix
==========================

What was tested
---------------
- Changed the classifier truth table first so bare TypeError("fetch failed")
  and bare TypeError("Unable to connect") must be non-retryable.
- Observed that test fail against the old implementation.
- Removed message-only classification while preserving explicit connection
  codes on the error and its cause.
- Changed the mailbox fallback integration regression to throw the exact
  ambiguous TypeError("fetch failed") and assert one prompt call.
- Ran:
  bunx bun@1.4.0 test packages/omo-opencode/src/shared/live-server-route.test.ts packages/omo-opencode/src/features/team-mode/tools/messaging.test.ts
  bunx bun@1.4.0 test packages/utils/src/prompt-async-gate*.test.ts
  bun run typecheck
  bunx bun@1.4.0 run build
- Drove the local dist/index.js through disposable OpenCode 1.18.4:
  docker run --rm --name omo-qa-5317-ambiguous-fetch \
    -w /workspaces/oh-my-openagent/.local-ignore/qa-5317 \
    -v "$PWD:/workspaces/oh-my-openagent" \
    -e OPENCODE_CONFIG_DIR=/workspaces/oh-my-openagent/.local-ignore/qa-5317/opencode-config \
    omo-qa opencode run \
    "OMO5317_AMBIGUOUS_FETCH: exercise safe mailbox fallback." \
    --format json --model qa-mock/qa-model

What was observed
-----------------
- RED: 30 pass, 2 fail. Both unqualified TypeErrors were incorrectly
  classified as definitely pre-send.
- GREEN classifier and messaging tests: 72 pass, 0 fail.
- Prompt-gate family: 70 pass, 0 fail.
- Typecheck: exit 0.
- CI-Bun build: exit 0.
- Real tool sequence:
  team_create:completed
  team_status:completed
  team_status:completed
  team_send_message:completed
  team_send_message:completed
  team_status:completed
  team_status:completed
- Final model text: OMO5317_REAL_PATH_OK memberResponses=2
- NDJSON error events: 0.
- Host OpenCode session count: 7933 before, 7933 after.

Why it is enough
----------------
The unit truth table distinguishes definite connect-phase failures from
ambiguous fetch failures. The team messaging regression proves the classifier
is applied at the actual durable mailbox fallback and does not replay a
possibly accepted request. The real OpenCode run confirms the registered tool
surface still delivers both messages after the fix.

What was omitted
----------------
- Credentials, host configuration, headers, and complete raw logs.
- Full CI reruns are delegated to the PR checks after this commit is pushed.
