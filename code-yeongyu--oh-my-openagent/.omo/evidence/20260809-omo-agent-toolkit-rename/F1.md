# F1 - Plan-compliance re-audit

Audited committed branch `feat/omo-agent-toolkit-rename` at `HEAD=0018c0687c101602316c7a5b2ee489e722a90636` against `origin/dev...HEAD`.

The worktree already contained generated-file dirt when this re-audit began. Source conclusions are pinned to committed `HEAD`; executable checks used the checked-out tree after the relevant generated builds completed. This audit changed only this `F1.md` verdict file.

## Must have

| requirement (quoted from the plan) | independently re-verified evidence | PASS or FAIL |
|---|---|---|
| Root npm packages gain `omo-agent-toolkit`, drop `omo`, preserve surviving aliases, and add `OMO_EDITION` routing. | `jq -c '.bin' package.json` returned exactly the five root aliases with `omo-agent-toolkit`; `jq -e '.bin.omo' package.json` exited 1; `bin/omo.js` and `bin/omo-agent-toolkit.js` are absent. Routing is at `packages/omo-opencode/src/cli/cli-program.ts:56-62` and `packages/omo-opencode/src/cli/doctor/framework/doctor-target.ts:3-6`. Independent source-entry execution for all four surviving aliases returned exit 0 for `--version` and `doctor --help`. After applying the old-command -> new-command rename and ignoring only horizontal padding before the unchanged inline comment, all four help outputs were equal. | **PASS** |
| Codex `linkRootRuntimeBin()` writes only `omo-agent-toolkit`, exports invocation and edition, deletes generated legacy wrappers, and preserves user-owned `omo`. | Implementation: `packages/omo-codex/src/install/codex-cache-bins.ts:56-84,207-230`; exports: `codex-cache-runtime-wrapper.ts:22-23,71-72`. Independent focused tests passed 15/15, including regular generated deletion, unmarked preservation, marker-bearing symlink deletion while preserving its target, unmarked-symlink preservation, and dangling-symlink tolerance. The regenerated installer contains the same symlink-aware code at `packages/omo-codex/scripts/install-dist/install-local.mjs:6948-6968`. Sequential isolated installer QA passed 13/13. | **PASS** |
| Senpi adapter uses the three-link toolkit-first resolver, never PATH-resolves bare `omo`, and launches `.js` through `process.execPath`. | Exact implementation is at `packages/omo-senpi/src/components/ulw-loop/omo-command.ts:17-40`. Independent scoped execution passed the `.js` platform cases, all resolver precedence cases, the PATH-only-bare-`omo` null case, and the real PATH toolkit invocation case. | **PASS** |
| Every agent-facing `omo ulw-loop` / `omo boulder` emit site migrates through a categorized audit while old input remains accepted. | The audit itself is now fixed and green: `bun test script/agent-command-string-audit.test.ts` passed, `script/agent-command-string-audit.test.ts:9,16-18` excludes its allowlist, and the allowlist has 0 `emit-migrate` and 0 `test-expectation` entries. The eight Ultrawork TOML hits are gone. However, an executable Senpi skill still resolves and assigns the removed command: `packages/omo-senpi/skills/ulw-loop/references/full-workflow.md:88` runs `command -v omo`, `:89` assigns `ULW_LOOP_CLI=omo`, and `:91` probes `$HOME/.local/bin/omo` and `$CODEX_HOME/bin/omo`. Old/new input parser tests do pass, but the conjunctive emit/runtime migration requirement is not complete. | **FAIL** |
| Doctor uses the post-removal bin set, warns on generated stale `omo`, stays silent for user-owned `omo`, honors `OMO_EDITION`, and uses the renamed bootstrap label. | Expected bins: `packages/omo-opencode/src/cli/doctor/checks/codex.ts:31-40`; canonical/stale checks: `codex-runtime-wrapper.ts:22-48`; routing: `doctor-target.ts:3-6`; bootstrap label: `packages/omo-codex/plugin/components/bootstrap/src/setup.ts:243-252`. Independent doctor/installer/Senpi scoped run passed 230/230, including marker warning, unmarked silence, and Codex routing. | **PASS** |
| Release workflow, docs, README, and CHANGELOG tell the complete removal story, including the end-state composition `omo-ai = senpi + packages/*senpi*`. | Workflow assertions are present at `.github/workflows/publish.yml:824-868`; `bunx actionlint .github/workflows/publish.yml` was clean; markdown and workflow tests passed. Root docs have no command-pattern hits. But the explicitly required composition is still absent. `docs/reference/cli.md:20` says, `The former omo bin was removed ... reserved for the future native edition (npm omo-ai), which is not shipped here.` `CHANGELOG.md:22` likewise says only, `The omo name is reserved for the future native edition (npm omo-ai), which is not shipped in this release.` Neither says `senpi + packages/*senpi*`; targeted `rg 'senpi.*omo-ai|omo-ai.*senpi|packages/\*senpi\*' docs README.md CHANGELOG.md` found no composition. | **FAIL** |
| Existing npm and Codex installs migrate automatically. | Sequential `lead-npm-qa.sh` passed 10/10: published latest first linked `omo`, then candidate install pruned it and linked `omo-agent-toolkit`. Installer and session bootstrap share `linkRootRuntimeBin()` at `packages/omo-codex/src/install/install-codex.ts:103` and `packages/omo-codex/plugin/components/bootstrap/src/setup.ts:235`; sequential isolated Codex QA passed generated-wrapper cleanup. | **PASS** |

## Must NOT have / guardrails

| requirement (quoted from the plan) | independently re-verified evidence | PASS or FAIL |
|---|---|---|
| No `packages/omo-native`. | `git diff --name-only origin/dev...HEAD | rg '^packages/omo-native(/|$)'` produced no output. | **PASS** |
| No `omo-ai` publish/install implementation, beta gating, or trusted-publisher work. | Added-line searches over workflows/docs for `npm publish ... omo-ai`, `npm install -g omo-ai`, beta dist-tag gating, and trusted-publisher text produced no implementation hits. The only `omo-ai` additions are context prose. | **PASS** |
| No Senpi dependency pin bump. | `git diff origin/dev...HEAD -- package.json bun.lock 'packages/*/package.json' | rg '^[+-][^+-].*(senpi.*[0-9]|@senpi)'` produced no output. | **PASS** |
| No legacy executable `omo` remnant beyond the explicitly tolerated parser/override/user-file/reserved-name cases. | **Still violated.** Exact active remnants: `packages/omo-senpi/skills/ulw-loop/references/full-workflow.md:88-91` resolves/assigns/probes legacy `omo`. Additional shipped support/diagnostic instructions recommend the dead command: `.agents/skills/codex-qa/SKILL.md:72`, `packages/omo-codex/README.md:52`, and `script/install-codex-dev.ts:45` say `omo get-local-version`; `packages/omo-senpi/plugin/README.md:46` says `omo install --platform=senpi`; `packages/omo-opencode/src/cli/doctor/checks/tools.ts:40` and `codex-components.ts:97` say `omo doctor`. These are not input compatibility literals. | **FAIL** |
| No CLI deprecation branch, `bin/omo.js`, or Codex forwarder. | `.bin.omo` is absent, both dedicated entry files are absent, `git diff origin/dev...HEAD -- bin/oh-my-opencode.js` is empty, and `linkRootRuntimeBin()` writes only `omo-agent-toolkit`. | **PASS** |
| No PATH lookup of bare `omo` in the Senpi resolver. | `packages/omo-senpi/src/components/ulw-loop/omo-command.ts:30-40` looks up only `omo-agent-toolkit`; the PATH-only-bare-`omo` test passed. | **PASS** |
| No npm package rename. | Only root `package.json` changed; base and HEAD both report `.name == "oh-my-opencode"`. The lazycodex publish transformation remains covered by passing workflow tests. | **PASS** |
| No marketplace identity rename. | `packages/omo-opencode/src/cli/doctor/checks/codex.ts:86,136,145` and `README.md:166` retain marketplace `sisyphuslabs`, plugin `omo`, and `omo@sisyphuslabs`. | **PASS** |
| No OMO_* env rename. | Source diff shows additive `OMO_EDITION` and `OMO_AGENT_TOOLKIT_BIN`; `OMO_BIN` remains at `omo-command.ts:35`. No existing public OMO_* setting was renamed. | **PASS** |
| No `.omo/` directory or `omo.json` config rename. | `git diff --name-status origin/dev...HEAD | rg '^R.*(\.omo/|omo\.json)'` produced no output. | **PASS** |
| No per-component bin rename. | No component `package.json` changed. The expected set at `codex.ts:33-39` keeps `omo-rules`, `omo-lsp`, `omo-comment-checker`, `omo-ultrawork`, `omo-start-work-continuation`, `omo-telemetry`, and `omo-git-bash-hook`; isolated Codex QA retained component bins and compatibility aliases. | **PASS** |
| No Codex hook-wiring changes. | `git diff --name-only origin/dev...HEAD` matched no `.codex-plugin/plugin.json`, `hooks/hooks.json`, or `plugin/hooks/*.json`. Isolated Codex QA also reported hook hashes byte-identical across installs. | **PASS** |
| No hand-edited generated artifacts. | The changed generated installer passed generated-bundle tests and contains the source symlink-reclamation logic. Codex sync/build tests and the full `test:codex` gate passed; the changed Senpi extension is generated from its source build path. No platform launcher package changed. | **PASS** |
| No changes to `../senpi`. | Every changed path is inside this repository; `git diff --name-only origin/dev...HEAD | rg '^\.\./senpi|^senpi/'` produced no output. | **PASS** |
| Do not drop old-form input acceptance. | Old and new forms remain together at `packages/omo-codex/plugin/components/ulw-loop/src/steering.ts:179` and `codex-hook.ts:98`; the independent scoped test run passed the parser equivalence cases. | **PASS** |

## Success criteria

| requirement (quoted from the plan) | independently re-verified evidence | PASS or FAIL |
|---|---|---|
| Fresh packed-root install has exactly five bins, no `omo`, working toolkit version, best-effort notice, and unchanged lazycodex-ai two-bin publication. | Sequential npm QA passed the exact five-bin set, `omo` absence, notice, and cleanup. A separate packed install executed `omo-agent-toolkit --version` with exit 0 and stdout `4.19.4`. Workflow tests passed for the lazycodex publication shape, but neither `task-6.txt` nor the re-audit actually packs the transformed lazycodex-ai package and compares its two-entry bin map to a pre-change package baseline as required. | **FAIL** |
| Four surviving aliases preserve output/exit behavior after rename normalization; no notice branch; `.bin.omo` absent. | All eight source-entry executions exited 0. For each alias the only help diff after command rename was spaces before the same `# Codex/LazyCodex diagnostics only` comment; alignment-aware normalization was equal for all four. `--version` stayed unchanged. No CLI notice branch exists and `.bin.omo` is absent. | **PASS** |
| Upgrade-in-place proves npm prunes `omo` and adds `omo-agent-toolkit`. | Sequential `lead-npm-qa.sh` installed published latest, observed its `omo`, installed the candidate over it, then passed both prune and canonical-link assertions. | **PASS** |
| Codex install produces only the canonical wrapper, reclaims generated regular files and symlinks, preserves user-owned entries, keeps hook/bin set, and is idempotent. | Focused link tests passed 15/15, including all symlink cases. Sequential isolated installer QA passed 13/13 for canonical exports, regular marker deletion, unmarked preservation, hook identity, bin state, and idempotency. The generated bundle carries the symlink-aware implementation. | **PASS** |
| Doctor is correct for fresh, migrated, marker-stale, unmarked-user, and Codex-edition states. | Independent doctor/installer scoped run passed 230/230; relevant cases include missing canonical detection, marker-bearing legacy warning, unmarked silence, and `OMO_EDITION=codex` routing. | **PASS** |
| Categorized audit is green with no emit/test entries; every emitted instruction uses the canonical command; old input remains accepted. | Audit is green and its self-scan bug is fixed; TOMLs are migrated; parser compatibility passes. The criterion still fails because `packages/omo-senpi/skills/ulw-loop/references/full-workflow.md:88-91` actively resolves/assigns legacy `omo`, and shipped support/doctor instructions listed above still emit dead `omo ...` commands. | **FAIL** |
| Publish smoke asserts canonical presence, `omo` absence, generated removal, user preservation, and actionlint; next release run passes. | Static workflow assertions exist at `.github/workflows/publish.yml:824-868`; workflow tests and actionlint passed. There is still no actual post-merge published release run, which this criterion explicitly names as evidence. | **FAIL** |
| Docs and CHANGELOG carry the full removal story; no documentation suggests bare `omo`; no shell alias is recommended. | Root `README.md` and `docs/` have zero legacy command-pattern hits, CHANGELOG has the breaking migration entry, and no `alias omo=...` recommendation exists. The repo-wide criterion still fails because active shipped/support docs recommend dead commands at `.agents/skills/codex-qa/SKILL.md:72`, `packages/omo-codex/README.md:52`, and `packages/omo-senpi/plugin/README.md:46`. | **FAIL** |
| `bun test`, `bun run typecheck`, and `bun run test:codex` are green; no omo-ai/omo-native scope expansion. | Valid isolated executions: `bun run typecheck` exit 0; `bun run test:codex` 519 pass / 0 fail; `bun test` 13454 pass / 5 skip / 0 fail. Scope greps found no omo-native path, omo-ai publication, Senpi pin bump, or hook-wiring change. | **PASS** |
| Naming end-state is documented everywhere, including `omo-ai = senpi + packages/*senpi*`, and `omo` remains reserved. | `RESERVED_NESTED_BIN_NAMES` keeps both `omo` and `omo-agent-toolkit` at `packages/omo-codex/src/install/codex-cache-bins.ts:11-18`. The composition text is absent from `docs/reference/cli.md:20` and `CHANGELOG.md:22`, and active legacy command instructions remain elsewhere. | **FAIL** |

## Resolution of the six previous blockers

| previous blocker | resolution | evidence |
|---|---|---|
| 1. Audit scans its own allowlist. | **FIXED** | `script/agent-command-string-audit.test.ts:9,16-18` explicitly excludes `script/agent-command-string-audit.allowlist.json`; independent audit test passed. |
| 2. Eight Ultrawork agent TOML emissions still use `omo ulw-loop`. | **FIXED** | Targeted `rg` over `packages/omo-codex/plugin/components/ultrawork/agents/*.toml` returned no legacy `omo ulw-loop|boulder` hits; allowlist counts are now `emit-migrate=0`, `test-expectation=0`, `docs=7`. |
| 3. Senpi skill resolves/assigns legacy `omo`. | **STILL-BROKEN - THE PREVIOUS AUDIT WAS RIGHT** | Exact evidence: `packages/omo-senpi/skills/ulw-loop/references/full-workflow.md:88` has `command -v omo`; `:89` has `ULW_LOOP_CLI=omo`; `:91` probes `$HOME/.local/bin/omo` and `$CODEX_HOME/bin/omo`. The claimed empty grep missed these non-`omo <verb>` executable forms. |
| 4. Four normalized alias outputs differ in spacing. | **WITHDRAWN-I-WAS-WRONG** | Independent comparison showed exactly one changed line per alias: the padding before the unchanged inline comment shrank after the command became longer. Alignment-aware normalization was equal for all four, exits were unchanged, and no semantics changed. |
| 5. Aggregate task-6 evidence is missing. | **FIXED** | `.omo/evidence/20260809-omo-agent-toolkit-rename/task-6.txt` now exists and records npm/Codex isolated smokes, all three gates, scope proofs, cleanup, and residual risk. Independent sequential npm QA passed 10/10 and Codex QA passed 13/13. |
| 6. Required `omo-ai = senpi + packages/*senpi*` documentation is absent. | **STILL-BROKEN - THE PREVIOUS AUDIT WAS RIGHT** | `docs/reference/cli.md:20` says only that `omo` is reserved for a future native edition `(npm omo-ai)`. `CHANGELOG.md:22` says the same. Neither quotes or describes `senpi + packages/*senpi*`; targeted composition search returned empty. |

## Verdict

**REJECT**

Blocking failures remain:

1. The Senpi workflow skill still performs executable legacy-`omo` discovery and assignment at `packages/omo-senpi/skills/ulw-loop/references/full-workflow.md:88-91`.
2. Shipped support and doctor surfaces still recommend removed commands (`omo get-local-version`, `omo install`, and `omo doctor`) at the exact locations listed above.
3. The plan-required naming composition `omo-ai = senpi + packages/*senpi*` is still not documented; the cited CLI and CHANGELOG lines do not say it.
4. The transformed lazycodex-ai package was not actually packed/baseline-compared, and the real post-merge publish smoke has not run, so those explicit success criteria remain unproven.
