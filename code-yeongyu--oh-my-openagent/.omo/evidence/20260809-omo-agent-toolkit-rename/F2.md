# F2 — Code-Quality Review: omo-agent-toolkit rename

**Worktree:** `.local-ignore/worktrees/omo-agent-toolkit-rename` (branch `feat/omo-agent-toolkit-rename`, 7 commits on `origin/dev`)
**Diff under review:** `git diff origin/dev...HEAD` (142 files; reviewable source diff excluding `.omo` evidence artifacts: 3,323 lines)
**Reviewer:** senpi-task child `st_019fe5d1` — read-only analysis, all claims verified by running the command.

---

## 1. Typecheck — REAL RESULT

Command: `bun run typecheck` (tsgo root + `typecheck:script` + `typecheck:packages`, 29 package projects)

```
TYPECHECK_EXIT=0
```

**PASS.** Zero diagnostics across root, `script/`, and all package tsconfigs.

## 2. Scoped tests — REAL RESULT

Command: `bun test bin script packages/omo-opencode/src/cli packages/omo-senpi/src/components/ulw-loop`

```
TEST_EXIT=1
 1189 pass
 1 fail
 2929 expect() calls
Ran 1190 tests across 186 files. [11.83s]
(fail) agent command string audit > #given tracked source files #when legacy agent and human commands are scanned #then every hit is categorized
```

**FAIL — one red test, reproduced standalone** (`bun test script/agent-command-string-audit.test.ts` → 0 pass / 1 fail, so it is not ordering or flake):

```
@@ collectHits() vs allowlist @@
+   "script/agent-command-string-audit.allowlist.json:5: omo ulw-loop",
+   ... (33 hits total, ALL inside script/agent-command-string-audit.allowlist.json itself)
- Expected  - 0
+ Received  + 33
    at script/agent-command-string-audit.test.ts:57:27
```

**Root cause:** the audit gate scans every `git ls-files` tracked file for `\bomo (ulw-loop|boulder|install|...)\b` but `isExcluded()` (script/agent-command-string-audit.test.ts:16-22) does not exclude the allowlist file itself. Every allowlist entry literally contains a legacy command string, so the allowlist self-matches 33 times and the gate can never pass as shipped. The change set's final commit ("migrate remaining agent commands … with an audit gate") therefore lands a **permanently red gate** — exactly what the gate exists to prevent.

**Fix (one line):** add the allowlist to the exclusions in `isExcluded()`:

```ts
|| filePath === "script/agent-command-string-audit.allowlist.json"
```

(Alternative: skip `ALLOWLIST_PATH` in `collectHits()`. Excluding in `isExcluded` is simpler and matches the existing CHANGELOG/.omo pattern.)

---

## 3. Diff vs repo laws (AGENTS.md)

| Law | Result |
|---|---|
| NO `as any` | CLEAN — zero added (one grep false-positive: prose "alias anywhere" in a doc line) |
| NO `@ts-ignore` / `@ts-expect-error` | CLEAN — zero added |
| No suppressed lints | CLEAN — zero `eslint-disable` / equivalent added |
| No emojis in code | CLEAN — none added. (`⚡ ultraworking` in `packages/omo-senpi/plugin/extensions/omo.js` is pre-existing inside a checked-in minified build artifact; the diff only re-stamped the bundle hash and renamed strings.) |
| kebab-case file naming | CLEAN — all new files kebab-case (`bin-map.test.ts`, `agent-command-string-audit.test.ts`, `agent-command-string-audit.allowlist.json`, `postinstall.test.ts`) |
| factory `createXXX` pattern | N/A — no new component/factory introduced; `resolveOmoBin`/`toSpawnTarget` are pre-existing exports modified in place |
| barrel `index.ts` exports only | NOT INTRODUCED — `packages/omo-senpi/src/components/ulw-loop/index.ts` holds business logic, but that predates this diff (the hunk only swaps `omo` → `omo-agent-toolkit` strings). Pre-existing condition, not charged to this change set |
| ~200 LOC soft file limit | PRE-EXISTING OVERAGE WORSENED — `packages/omo-codex/src/install/codex-cache-bins.ts` grew 229 → 255. Already over the soft limit before; this diff adds 26 lines instead of extracting. Observation only (soft limit) |
| given/when/then with `#given`/`#when`/`#then` prefixes, NEVER AAA | ONE VIOLATION — see V2 below. No Arrange-Act-Assert anywhere |
| No empty catch blocks | CLEAN — new `removeGeneratedRuntimeWrapper` catch rethrows non-ENOENT; `readActiveStatus` catches log-and-return; bundle `catch{}` is minified pre-existing output |
| No catch-all util/helper/service files | CLEAN — none added |

### Violations

**V1 (BLOCKING) — script/agent-command-string-audit.test.ts:57** — shipped test fails at HEAD (see §2). The audit gate does not exclude its own allowlist file, producing 33 uncategorized self-hits. *Why it violates:* a change set may not land with a red scoped suite; a gate that cannot pass is worse than no gate. *Fix:* exclude `script/agent-command-string-audit.allowlist.json` in `isExcluded()` (line 16-22), then re-run the suite to green.

**V2 (minor) — packages/omo-opencode/src/cli/install-platform-resolution.test.ts:170,180,190,200,208** — the five new `OMO_EDITION install routing` tests use plain-English names with bare `// when` / `// then` comments and carry **no `#given`/`#when`/`#then` prefixes anywhere**, which the law requires. (The sibling new tests added in `doctor-target.test.ts` in the same diff do carry the prefixes, so the diff is internally inconsistent; the plain style matches only this file's older pre-existing tests.) *Fix:* rename to the `#given … #when … #then …` form, e.g. `#given OMO_EDITION=codex #when resolving install args for a non-codex invocation #then the platform defaults to codex`.

## 4. AI-slop comment check

**V3 (minor) — script/bin-map.test.ts:1-2**

```ts
// script/bin-map.test.ts
// Shape tests for the root package.json bin map (omo -> omo-agent-toolkit rename)
```

Line 1 merely restates the filename; line 2 narrates the change ("rename") instead of stating a durable contract. Both are noise. *Fix:* delete both lines (the describe string already says what is under test).

**Checked and NOT flagged (substantive WHY-comments, keep them):**
- `packages/omo-senpi/src/components/ulw-loop/omo-command.ts` — the "Deliberately NO PATH lookup of the bare name `omo`" block and the `.js` spawn note explain non-obvious safety rationale.
- `postinstall.mjs` — npm ≥7 lifecycle-output hiding note explains why the notice is best-effort.
- `.github/workflows/publish.yml` — upgrade-simulation contract comment documents a safety invariant (marked vs user-owned legacy wrapper).
- `packages/omo-codex/plugin/components/bootstrap/src/setup.ts` — degraded-path comment (string-updated only).
- `.omo/evidence/20260809-omo-agent-toolkit-rename/lead-codex-qa.sh` and `lead-npm-qa.sh` — ISOLATION CONTRACT headers confirmed present and justified as safety documentation per review brief; not flagged.
- Bare `// given`/`// when`/`// then` inline markers under `#given`-prefixed test names (codex-cache-bins.test.ts, codex.test.ts, doctor-target.test.ts, bin-map.test.ts) match each file's pre-existing local convention; noted, not charged.

## 5. Tautology assessment — NEW test files (per test)

**script/bin-map.test.ts** — 3 tests, all genuine change-detectors:
1. `…the omo bin entry is absent` — FAILS if `omo` is re-added to the bin map. Real.
2. `…omo-agent-toolkit points at the shared entry` — FAILS if the entry is removed or repointed. Real.
3. `…the four surviving aliases keep the shared entry` — FAILS if any of the four aliases drifts. Real.

**script/agent-command-string-audit.test.ts** — 1 test:
1. `…every hit is categorized` — NOT tautological; it is a live gate (proven by the fact that it currently fails). However, as shipped it **cannot pass** (V1): its failure mode right now is self-scan, not a real regression. The `emit-migrate`/`test-expectation` `toEqual([])` assertions are intentional debt-gates that fail if anyone files new entries there — by design.

**postinstall.test.ts** — 3 tests, all execute the real `postinstall.mjs` in an isolated fixture HOME:
1. `announces the omo-agent-toolkit rename exactly once` — FAILS if the notice is removed, reworded, or printed twice. Real.
2. `never fails the install regardless of platform binary resolution` — FAILS if postinstall exits non-zero in the fixture env. Genuine assertion, but the name overclaims: only one environment shape is exercised, "regardless of platform binary resolution" is not varied. Minor naming issue, not a tautology.
3. `stays idempotent across repeated runs` — misnamed: it asserts each of two runs prints the notice exactly once (run-to-run consistency). True idempotency (second run prints nothing) would FAIL this test. Assertions can fail; not tautological. Recommend renaming to `prints the notice exactly once on every run`.

**Tests that cannot fail: none.** Every new test names a behavior whose regression would turn it red.

---

## Verdict

One blocking defect (V1: the audit-gate test is red at HEAD by self-scan, confirmed by real test runs, full-suite and standalone) plus two minor violations (V2 missing `#given/#when/#then` prefixes in the new install-platform-resolution tests, V3 slop header comments in bin-map.test.ts). Everything else — typecheck, remaining 1,189 tests, banned-pattern scan, naming, catches, comment quality, generated artifacts — is clean.

**REJECT — fix V1 (exclude `script/agent-command-string-audit.allowlist.json` in `isExcluded()`, suite must go 1190/1190 green), V2, and V3, then re-submit.**
