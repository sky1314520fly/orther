# F4 — Scope fidelity (executable Must-NOT-Have audit)

```
verdict: APPROVE
merge_base: 15ad6e617aa9eb8f7ed277b5d89357ab66b191e7
```

- HEAD: `b084aeeebe93438277e5bf39c29cd60cb20c9bf0`
- Worktree: `/Volumes/mengmotaStorage/local-workspaces/omo-wt/feat-telemetry-parallel-latency`
- **Diff scope used for every check:** `git diff 15ad6e61 HEAD` (= `git diff $(git merge-base origin/dev HEAD) HEAD`).
  `git diff origin/dev HEAD` is WRONG here: the branch is ~22 commits behind `origin/dev`, so that form
  attributes everything `dev` advanced by since branching to this branch and manufactures false violations.
- Read-only run: no source, test, or doc file was modified. Two throwaway scripts were created in the
  worktree root and deleted in the same command (`f4-schema-check.ts`, `f4-c7-c9.ts`); `git status` is clean
  apart from this evidence file.

---

## 1. `turn_completed` schema UNCHANGED

Command:
```
git diff 15ad6e61 HEAD -- packages/omo-senpi/src/components/telemetry/product-identity.ts
git diff 15ad6e61 HEAD -- packages/omo-senpi/src/components/telemetry/product-identity.ts \
  | grep -n -E '^[+-]' | grep -i 'turn_completed'; echo "exit=$?"
```

Output — the full diff of the schema file is one purely additive hunk:
```
diff --git a/packages/omo-senpi/src/components/telemetry/product-identity.ts b/packages/omo-senpi/src/components/telemetry/product-identity.ts
index 801d1c6d2..a75a74bcc 100644
--- a/packages/omo-senpi/src/components/telemetry/product-identity.ts
+++ b/packages/omo-senpi/src/components/telemetry/product-identity.ts
@@ -132,6 +132,24 @@ export const OMO_NATIVE_EVENT_SCHEMAS = Object.freeze({
     "$session_id": STRING_PROPERTY,
     feature: enumProperty(["goal_tool", "team_create", "memory_tool"] as const),
   }),
+  parallelism_summary: Object.freeze({
+    "$session_id": STRING_PROPERTY,
+    clock_anomalies: NUMBER_PROPERTY,
+    dropped_calls: NUMBER_PROPERTY,
+    eval_only_duration_ms: NUMBER_PROPERTY,
+    eval_only_waves: NUMBER_PROPERTY,
+    incomplete_calls: NUMBER_PROPERTY,
+    measured_turn_duration_ms_total: NUMBER_PROPERTY,
+    mixed_waves: NUMBER_PROPERTY,
+    modeled_wallclock_saved_ms: NUMBER_PROPERTY,
+    non_eval_joined_calls: NUMBER_PROPERTY,
+    non_eval_saved_round_trips: NUMBER_PROPERTY,
+    non_eval_wave_size_histogram: STRING_PROPERTY,
+    non_eval_waves_multi: NUMBER_PROPERTY,
+    non_eval_waves_total: NUMBER_PROPERTY,
+    schema_kind: enumProperty(["parallelism_v1"] as const),
+    upper_bound_saved_ms: NUMBER_PROPERTY,
+  }),
 } as const)

 export const OMO_NATIVE_PROPERTY_ALLOWLISTS = Object.freeze(Object.fromEntries(
```
Grep for `turn_completed` among added/removed lines:
```
exit=1        (grep found no match)
```
Zero added or removed lines touch the `turn_completed` entry. The entry is not even in the hunk context.
**PASS**

---

## 2. Zero changes under `packages/telemetry-core/`

Command:
```
git diff --stat 15ad6e61 HEAD -- packages/telemetry-core/; echo "(end)"
```
Output:
```
(end)
```
Empty stat = zero changed files. **PASS**

---

## 3. Zero changes under `packages/omo-codex/` and `packages/omo-opencode/`

Command:
```
git diff --stat 15ad6e61 HEAD -- packages/omo-codex/ packages/omo-opencode/; echo "(end)"
```
Output:
```
(end)
```
**PASS**

---

## 4. Zero changes under `packages/omo-senpi/plugin/extensions/` (generated build artifacts)

Command:
```
git diff --stat 15ad6e61 HEAD -- packages/omo-senpi/plugin/extensions/; echo "(end)"
```
Output:
```
(end)
```
**PASS**

---

## 5. No code-mode / K / compression-ratio metric

Commands:
```
git diff 15ad6e61 HEAD -- packages/ docs/ | grep -n -E '^\+' \
  | grep -E 'compression_ratio|code_mode_k|codemode_k|internal_ops|logical_op'; echo "exit=$?"
grep -rn -E 'compression_ratio|code_mode_k|codemode_k|internal_ops|logical_op' \
  packages/omo-senpi/src/components/telemetry/ docs/reference/senpi-telemetry.md; echo "exit=$?"
```
Output:
```
=== C5: forbidden metric identifiers in changed source ===
exit=1
=== C5b: repo-wide grep in changed dir ===
exit=1
```
Both greps return no matches (exit 1 = no lines selected), across the added lines of the diff and across the
whole changed telemetry directory plus the regenerated doc. **PASS**

---

## 6. No `max(dᵢ)`-based savings formula; `savings-math.ts` uses `spanMs`

Command A — search every telemetry source for a spread `Math.max` over durations:
```
grep -rn -E 'Math\.max\(\.\.\.' packages/omo-senpi/src/components/telemetry/; echo "exit=$?"
```
Output:
```
exit=1        (no match anywhere, including tests)
```
The only `Math.max` in `savings-math.ts` is `Math.max(wave.maxConcurrency - 1, 0)`, a two-arg clamp on the
round-trip count — not a max over durations.

Command B — confirm the modeled formula reads `spanMs`:
```
cat packages/omo-senpi/src/components/telemetry/savings-math.ts
```
Relevant output:
```
export function modeledWallClockSavedMs(wave: MeasurableWave): ModeledSavedMs {
  const durations = usableDurations(wave.calls)
  if (durations.length <= 1 || !Number.isFinite(wave.spanMs)) {
    return { label: "modeled", valueMs: 0 }
  }
  return { label: "modeled", valueMs: sum(durations) - wave.spanMs }
}
```
Executable confirmation on the plan's chained regression case A(0-5) B(4-9) C(8-12):
```
C7 modeledWallClockSavedMs      = {"label":"modeled","valueMs":2}
```
2 ms (span basis), not 9 ms (`max` basis). **PASS**

---

## 7. `(N−1)×mean` is NOT exposed as a default/primary metric

Command A — every export of `savings-math.ts`:
```
grep -rn -E 'export (function|const|type)' packages/omo-senpi/src/components/telemetry/savings-math.ts
```
Output:
```
savings-math.ts:20:export type MeasurableCall = {
savings-math.ts:25:export type MeasurableWave = {
savings-math.ts:31:export type ModeledSavedMs = {
savings-math.ts:36:export type UpperBoundSavedMs = {
savings-math.ts:41:export function modeledWallClockSavedMs(wave: MeasurableWave): ModeledSavedMs {
savings-math.ts:49:export function upperBoundSavedMs(wave: MeasurableWave): UpperBoundSavedMs {
savings-math.ts:56:export function savedRoundTrips(waves: readonly MeasurableWave[]): number {
```

Command B — where the formula literally lives:
```
grep -rn -E '\- 1\) \* mean|length - 1\) \*' packages/omo-senpi/src/components/telemetry/*.ts | grep -v '\.test\.ts'
```
Output:
```
savings-math.ts:9: * `(N - 1) * mean(duration)` is an upper bound, not a measurement, and is exposed
savings-math.ts:53:  return { label: "upper_bound", valueMs: (durations.length - 1) * mean }
```
The single occurrence is inside `upperBoundSavedMs` — the only export whose NAME carries `upperBound`, and
whose return type `UpperBoundSavedMs` pins the literal `label: "upper_bound"`.

Command C — every consumer:
```
grep -rn 'upperBoundSavedMs\|upper_bound' packages/omo-senpi/src/components/telemetry/*.ts | grep -v '\.test\.ts'
```
Output:
```
omo-native-parallel-summary.ts:33:import { modeledWallClockSavedMs, savedRoundTrips, upperBoundSavedMs } from "./savings-math"
omo-native-parallel-summary.ts:102:    upper_bound_saved_ms: sumBy(nonEvalWaves, (wave) => upperBoundSavedMs(wave).valueMs),
product-identity.ts:151:    upper_bound_saved_ms: NUMBER_PROPERTY,
savings-math.ts:10: * only through `upperBoundSavedMs`. The two results carry distinct literal labels
savings-math.ts:37:  readonly label: "upper_bound"
savings-math.ts:38:}
savings-math.ts:49:export function upperBoundSavedMs(wave: MeasurableWave): UpperBoundSavedMs {
savings-math.ts:51:  if (durations.length <= 1) return { label: "upper_bound", valueMs: 0 }
savings-math.ts:53:  return { label: "upper_bound", valueMs: (durations.length - 1) * mean }
```
Its only wire surface is the schema property `upper_bound_saved_ms`, which carries `upper_bound` in its name.

Command D — executable proof that no non-upper-bound export returns the value:
```
bun run f4-c7-c9.ts
```
Output:
```
C7 modeledWallClockSavedMs      = {"label":"modeled","valueMs":2}
C7 upperBoundSavedMs            = {"label":"upper_bound","valueMs":9.333333333333334}
C7 savedRoundTrips              = 1
C7 does any non-upper-bound export return (N-1)*mean = 9.333333333333334 ? false
```
**PASS**

---

## 8. Every tool-call-derived COUNT property carries a domain prefix

Command — the schema's property list is extracted programmatically from the compiled module (not read by eye)
and classified against `^(non_eval_|eval_only_|mixed_)`:
```
bun run f4-schema-check.ts     # imports OMO_NATIVE_EVENT_SCHEMAS.parallelism_summary
```
Output:
```
$session_id                       not a COUNT (duration/identity/enum)
clock_anomalies                   quality counter (pre-classification, plan-enumerated, prefix N/A)
dropped_calls                     quality counter (pre-classification, plan-enumerated, prefix N/A)
eval_only_duration_ms             not a COUNT (duration/identity/enum)
eval_only_waves                   TOOL-CALL COUNT / domain-prefixed OK
incomplete_calls                  quality counter (pre-classification, plan-enumerated, prefix N/A)
measured_turn_duration_ms_total   not a COUNT (duration/identity/enum)
mixed_waves                       TOOL-CALL COUNT / domain-prefixed OK
modeled_wallclock_saved_ms        not a COUNT (duration/identity/enum)
non_eval_joined_calls             TOOL-CALL COUNT / domain-prefixed OK
non_eval_saved_round_trips        TOOL-CALL COUNT / domain-prefixed OK
non_eval_wave_size_histogram      TOOL-CALL COUNT / domain-prefixed OK
non_eval_waves_multi              TOOL-CALL COUNT / domain-prefixed OK
non_eval_waves_total              TOOL-CALL COUNT / domain-prefixed OK
schema_kind                       not a COUNT (duration/identity/enum)
upper_bound_saved_ms              not a COUNT (duration/identity/enum)

MISSING-PREFIX TOOL-CALL COUNTS: []
```

Classification basis (auditable, not discretionary): the plan's todo-5 line enumerates the intended schema and
explicitly names `incomplete_calls`/`clock_anomalies` as **품질 카운터 (quality counters)**, listed separately
from the `non_eval_`-prefixed tool-call-derived counts. `dropped_calls` belongs to the same class — it is the
`MAX_TRACKED_CALLS` overflow counter, incremented before any wave or eval classification exists:
```
if (observation.kind === "start") {
  counters.observedCalls += 1
  if (paired.length + pending.size >= MAX_TRACKED_CALLS) {
    counters.droppedCalls += 1
    continue
  }
```
A dropped call never reaches a bucket, so no `non_eval_`/`eval_only_`/`mixed_` domain exists to prefix it with;
prefixing it would assert a classification that was never computed. Every property that IS a bucketed
tool-call-derived count carries a domain prefix. **PASS**

---

## 9. Histogram carries NO `=` labels; positional encoding; fits under 64-char truncation

Command A — histogram construction:
```
grep -rn -E 'histogram|join\(":"\)|BUCKET' packages/omo-senpi/src/components/telemetry/*.ts | grep -v '\.test\.ts'
```
Output:
```
eval-classifier.ts:30:const WAVE_SIZE_BUCKET_MAXIMA = [1, 2, 3, 4, 8, 16, 32] as const
eval-classifier.ts:31:const HISTOGRAM_BUCKET_COUNT = WAVE_SIZE_BUCKET_MAXIMA.length + 1
eval-classifier.ts:49:  const histogram = new Array<number>(HISTOGRAM_BUCKET_COUNT).fill(0)
eval-classifier.ts:72:    if (size > 0) histogram[waveSizeBucketIndex(size)] += 1
eval-classifier.ts:80:      waveSizeHistogram: histogram.join(":"),
eval-classifier.ts:89:  const index = WAVE_SIZE_BUCKET_MAXIMA.findIndex((maximum) => size <= maximum)
eval-classifier.ts:90:  return index === -1 ? HISTOGRAM_BUCKET_COUNT - 1 : index
omo-native-parallel-summary.ts:98:    non_eval_wave_size_histogram: buckets.nonEval.waveSizeHistogram,
product-identity.ts:147:    non_eval_wave_size_histogram: STRING_PROPERTY,
```
The encoding is a bare `number[]` indexed by bucket, joined with `":"` — positional, no label text.

Command B — search for `=`-style labels:
```
grep -rn -E '"[a-z0-9_]+=' packages/omo-senpi/src/components/telemetry/*.ts | grep -v '\.test\.ts'; echo "exit=$?"
```
Output:
```
exit=1        (no match)
```

Command C — truncation limit in the privacy wrapper:
```
grep -rn -E 'slice\(0, *[0-9]+\)|MAX_STRING|64' packages/telemetry-core/src/events.ts
```
Output:
```
packages/telemetry-core/src/events.ts:125:        projected[key] = typeof value === "string" ? value.slice(0, 64) : value
```

Command D — actual worst-case width, executed against `MAX_TRACKED_CALLS`:
```
bun run f4-c7-c9.ts
```
Output:
```
C9 MAX_TRACKED_CALLS            = 2000
C9 histogram                    = 50:50:50:50:200:400:800:400
C9 length                       = 27 (limit 64)
C9 contains '='                 = false
C9 theoretical worst case       = 39 chars (8 buckets x 4 digits + 7 colons)
```
Documented worst case (8 buckets × 4 digits + 7 colons = 39) is under the wrapper's 64-char `slice`, and this
is pinned by tests:
```
product-identity.test.ts:110:  test("#given the tracked-call cap #when the widest wave histogram is encoded #then it stays inside the 64 character privacy limit", ...
product-identity.test.ts:121:    expect(worstCaseHistogram.length).toBeLessThanOrEqual(64)
omo-native-parallel-summary.test.ts:200:        expect(String(histogram).length).toBeLessThan(64)
omo-native-parallel-summary.test.ts:226:        expect(String(properties?.non_eval_wave_size_histogram).length).toBeLessThan(64)
```
**PASS**

---

## 10. `parallelism_summary` emitted AT MOST ONCE per session

Command A — locate every capture site:
```
grep -rn 'captureEvent(\|capture(' packages/omo-senpi/src/components/telemetry/omo-native-parallel-summary.ts \
                                    packages/omo-senpi/src/components/telemetry/omo-native-parallel.ts
```
Output:
```
omo-native-parallel-summary.ts:56:    captureEvent("parallelism_summary", properties)
```
Exactly one emission site in the entire feature. It lives inside a `session_shutdown` handler.

Command B — the guard:
```
sed -n '43,60p' packages/omo-senpi/src/components/telemetry/omo-native-parallel-summary.ts
```
Output:
```
  const { captureEvent, hashSessionId, ...telemetryOptions } = options
  const registry = telemetryOptions.registry ?? createParallelTelemetryRegistry()

  pi.on("session_shutdown", (_payload: unknown, eventContext: unknown): void => {
    const sessionId = extractSessionId(eventContext)
    if (sessionId === undefined) return
    const snapshot = registry.snapshot(sessionId)
    registry.clear(sessionId)
    if (snapshot === undefined) return
    const properties = buildParallelismSummary(snapshot, hashSessionId(sessionId))
    if (properties === undefined) return
    captureEvent("parallelism_summary", properties)
  })
```
The guard is consume-and-clear: `registry.clear(sessionId)` runs unconditionally **before** the emit, so a
second `session_shutdown` for the same session gets `snapshot === undefined` and returns without capturing.
No per-turn and no per-wave emission path exists — `session_shutdown` is the only subscription that captures.

Command C — the once-per-session bound is pinned by tests:
```
grep -rn -E 'toHaveLength\(1\)|second shutdown|exactly once' \
  packages/omo-senpi/src/components/telemetry/omo-native-parallel-summary.test.ts
```
Output:
```
:99:        expect(fx.captured).toHaveLength(1)
:270:      test("#then a second shutdown for the same session emits nothing", async () => {
:278:        expect(fx.captured).toHaveLength(1)
:296:      test("#then a shutdown mid turn still emits the completed calls exactly once", async () => {
:308:        expect(fx.captured).toHaveLength(1)
:321:        expect(fx.captured).toHaveLength(1)
```
Body of the double-shutdown regression test:
```
      test("#then a second shutdown for the same session emits nothing", async () => {
        // given
        const fx = fixture()
        await toolCall(fx, "s1", { toolCallId: "a", toolName: "bash", startMs: 1_000, endMs: 1_500 })
        // when
        await shutdown(fx, "s1")
        await shutdown(fx, "s1")
        // then
        expect(fx.captured).toHaveLength(1)
      })
```
Cannot fire more than once per session. **PASS**

---

## 11. Full changed-file list; nothing outside the allowed paths

Command:
```
git diff --name-only 15ad6e61 HEAD
```
Output:
```
.omo/evidence/telemetry-parallel-latency-v2/task-1.md
.omo/evidence/telemetry-parallel-latency-v2/task-2.md
.omo/evidence/telemetry-parallel-latency-v2/task-3.md
.omo/evidence/telemetry-parallel-latency-v2/task-4.md
.omo/evidence/telemetry-parallel-latency-v2/task-5.md
.omo/evidence/telemetry-parallel-latency-v2/task-6.md
docs/reference/senpi-telemetry.md
packages/omo-senpi/src/components/telemetry/eval-classifier.test.ts
packages/omo-senpi/src/components/telemetry/eval-classifier.ts
packages/omo-senpi/src/components/telemetry/omo-native-component.test.ts
packages/omo-senpi/src/components/telemetry/omo-native-component.ts
packages/omo-senpi/src/components/telemetry/omo-native-parallel-summary.test.ts
packages/omo-senpi/src/components/telemetry/omo-native-parallel-summary.ts
packages/omo-senpi/src/components/telemetry/omo-native-parallel.test.ts
packages/omo-senpi/src/components/telemetry/omo-native-parallel.ts
packages/omo-senpi/src/components/telemetry/product-identity.test.ts
packages/omo-senpi/src/components/telemetry/product-identity.ts
packages/omo-senpi/src/components/telemetry/savings-math.test.ts
packages/omo-senpi/src/components/telemetry/savings-math.ts
packages/omo-senpi/src/components/telemetry/wave-assembler.test.ts
packages/omo-senpi/src/components/telemetry/wave-assembler.ts

 21 files changed, 3499 insertions(+)
```
Note: 21 files, **3499 insertions, 0 deletions** — the branch is purely additive at merge-base scope.

Assertion command:
```
git diff --name-only 15ad6e61 HEAD | grep -v -E '^(packages/omo-senpi/src/components/telemetry/|docs/reference/senpi-telemetry\.md$|\.omo/evidence/)'; echo "outside-scope exit=$?"
```
Output:
```
outside-scope exit=1        (grep selected no lines -> zero files outside the allowed paths)
```
**PASS**

---

## Must-NOT-Have checklist (verbatim from the plan)

| # | Must-NOT-Have item (verbatim) | Check | Result |
| --- | --- | --- | --- |
| 1 | `turn_completed` 스키마·볼륨·샘플링 변경 **일체 금지** (사용자 명시). | 1, 10 | PASS |
| 2 | 코드모드 K/압축비 지표 **일체 금지** (사용자 명시). | 5 | PASS |
| 3 | `(N−1)×평균` 형태의 시간절감 지표 금지 — 상한으로만, 그것도 `_upper_bound` 이름일 때만 허용. | 7 | PASS |
| 4 | 웨이브 절감에 `max(dᵢ)` 기준 사용 금지 (span 기준만 허용). | 6 | PASS |
| 5 | 히스토그램 문자열에 라벨 포함 금지 — 위치 인코딩만. | 9 | PASS |
| 6 | `packages/telemetry-core/` 변경 금지 (harness-neutral 유지). | 2 | PASS |
| 7 | `packages/omo-codex/`, `packages/omo-opencode/` 변경 금지. | 3 | PASS |
| 8 | 프롬프트/경로/파일명/셀 소스 등 원문 전송 금지. | 8 (schema has no `_text`/`_path`/`_prompt` property; all properties are numbers, the positional histogram string, an enum, and the hashed `$session_id`) | PASS |
| 9 | 세션 median을 이벤트에 실어 보내는 것 금지. | 8 (no `median`/ratio property in the extracted property list) | PASS |
| 10 | 구현 중 스코프 확장 금지: 이 플랜에 없는 새 이벤트 추가 금지. | 1, 10, 11 (`parallelism_summary` is the only added schema entry; only one capture site) | PASS |
| — | `packages/omo-senpi/plugin/extensions/` generated artifacts untouched. | 4 | PASS |

## Verdict

**APPROVE.** All 11 executable checks pass at merge-base scope `15ad6e617aa9eb8f7ed277b5d89357ab66b191e7`.
No Must-NOT-Have item is violated. The branch is additive-only (3499 insertions, 0 deletions) and confined to
`packages/omo-senpi/src/components/telemetry/`, the regenerated `docs/reference/senpi-telemetry.md`, and
`.omo/evidence/`.
