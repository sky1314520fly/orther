# F3 — Real manual QA from a clean checkout

```
verdict: APPROVE
```

Verified independently of the implementers' worktree. Two branch revisions were verified
because the branch head advanced mid-run (see "Branch head moved mid-run" below):

- `bf954101a` — `feat(omo-senpi): emit one parallelism_summary per session` (head at task start)
- `1ac22d815` — `docs(evidence): record post-commit bundle verification transcript` (head at task end)

Both are green on the primary gates.

---

## Honest disclosure up front

Two things about this run must not be glossed over:

1. **Dependency provisioning is a shortcut, not a from-scratch install.** As instructed, I did
   NOT run `bun install` (it corrupted the shared store once already and other agents are
   working concurrently). I symlinked the main checkout's `node_modules` into the fresh tree,
   plus every per-package `node_modules`. **This therefore proves the branch's *source* is
   green against the store's current contents; it does NOT prove `bun install` from
   `bun.lock` would produce that same store.** A true from-scratch install remains unproven
   by this task.

2. **The literal `/tmp/f3-clean` path in the task spec is poisoned and produces a FALSE
   REJECT.** I ran it there first, exactly as specified, and typecheck failed. Root cause
   below. I re-ran the authoritative gates from a non-poisoned path. Both runs are recorded.

---

## PART A — Clean checkout gates

### A1. Fresh detached worktree (as specified, at `/tmp/f3-clean`)

```
$ cd /Volumes/mengmotaStorage/local-workspaces/omo && git worktree add --detach /tmp/f3-clean feat/telemetry-parallel-latency
Preparing worktree (detached HEAD bf954101a)
Updating files: 100% (8076/8076), done.
HEAD is now at bf954101a feat(omo-senpi): emit one parallelism_summary per session
```

### A2. Provisioning (symlink shortcut)

```
$ ln -s /Volumes/mengmotaStorage/local-workspaces/omo/node_modules /tmp/f3-clean/node_modules
$ ls -ld /tmp/f3-clean/node_modules
lrwxr-xr-x  1 yeongyu  wheel  58 Aug 16 18:12 /tmp/f3-clean/node_modules -> /Volumes/mengmotaStorage/local-workspaces/omo/node_modules
```

The root symlink alone was **not sufficient**. First run failed:

```
$ cd /tmp/f3-clean && bun test packages/omo-senpi/src/components/telemetry/
...
# Unhandled error between tests
-------------------------------
error: Cannot find package 'typebox' from '/private/tmp/f3-clean/packages/omo-senpi/src/components/memory/tools.ts'
-------------------------------

 131 pass
 1 fail
 1 error
 459 expect() calls
Ran 132 tests across 15 files. [3.72s]
EXIT=1
```

Per-package `node_modules` links had to be mirrored (the main checkout carries 28 of them;
`packages/omo-senpi/node_modules/typebox` is one). After mirroring all 28:

```
$ git -C /tmp/f3-clean status --porcelain | wc -l
28      # all entries are '?? .../node_modules' provisioning links only — no source/test/doc file touched
```

Re-run then passed:

```
$ cd /tmp/f3-clean && bun test packages/omo-senpi/src/components/telemetry/
 136 pass
 0 fail
 480 expect() calls
Ran 136 tests across 15 files. [2.80s]
EXIT=0
```

### A2b. `/tmp` is poisoned — typecheck false-REJECT and its root cause

At `/tmp/f3-clean`, typecheck FAILED:

```
$ cd /tmp/f3-clean && bun run --cwd packages/omo-senpi typecheck
$ tsgo --noEmit -p tsconfig.json
../../../../../Volumes/mengmotaStorage/local-workspaces/omo/packages/utils/src/runtime/file.ts(35,11): error TS2322:
  Type '() => Promise<ArrayBuffer | SharedArrayBuffer>' is not assignable to type '() => Promise<ArrayBuffer>'.
    Type 'ArrayBuffer | SharedArrayBuffer' is not assignable to type 'ArrayBuffer'.
      Type 'SharedArrayBuffer' is missing the following properties from type 'ArrayBuffer': resizable, resize, detached, transfer, transferToFixedLength
EXIT=2
```

I did **not** accept this at face value. Diagnosis:

- The failing file `packages/utils/src/runtime/file.ts` is **byte-identical between `dev` and the branch**:
  ```
  $ git diff 95fe3be52 bf954101a -- packages/utils/src/runtime/file.ts
  (empty)
  ```
- Main checkout and implementers' worktree both typecheck clean:
  ```
  $ cd /Volumes/mengmotaStorage/local-workspaces/omo && bun run --cwd packages/omo-senpi typecheck   -> MAIN_EXIT=0
  $ cd .../omo-wt/feat-telemetry-parallel-latency && bun run --cwd packages/omo-senpi typecheck      -> IMPL_WT_EXIT=0
  ```
- Isolated to `packages/utils` alone, same source + same tsconfig, differing only by tree:
  ```
  $ cd /Volumes/.../omo   && bun run --cwd packages/utils typecheck   -> MAIN_UTILS_EXIT=0
  $ cd /tmp/f3-clean      && bun run --cwd packages/utils typecheck   -> CLEAN_UTILS_EXIT=2 (same TS2322)
  ```
- Root cause found — module resolution walks up past the worktree into `/tmp`:
  ```
  $ cd /tmp/f3-clean/packages/utils && bun -e "console.log(Bun.resolveSync('@types/node/package.json', process.cwd()))"
  /private/tmp/node_modules/@types/node/package.json

  $ cat /private/tmp/node_modules/@types/node/package.json | grep '"version"'
      "version": "18.19.130",
  ```
  A stray, unrelated `/tmp/node_modules` (161 packages, some other project's install, dated
  Aug 16 14:49) shadows resolution for anything checked out under `/tmp`. Its `@types/node@18`
  predates the `ArrayBuffer` refinements (`resizable`/`resize`/`detached`/`transfer`), which is
  exactly the reported error. The repo pins `@types/node` 25.9.x/26.1.x in its own store.

**Conclusion: environmental, not branch-caused.** Any checkout of any branch under `/tmp`
would fail identically. I therefore re-ran the authoritative gates outside `/tmp`.

### A3. Authoritative gates — clean detached worktree, non-poisoned path

Ancestor-pollution check first (this is the thing that broke `/tmp`):

```
$ p=/Volumes/mengmotaStorage/local-workspaces/omo-wt-f3/f3-clean; while [ "$p" != "/" ]; do p=$(dirname "$p"); [ -d "$p/node_modules" ] && echo "POLLUTED: $p/node_modules"; done; echo "ancestor scan done"
ancestor scan done      # no polluting ancestor node_modules
```

Worktree + identical symlink provisioning:

```
$ git worktree add --detach /Volumes/mengmotaStorage/local-workspaces/omo-wt-f3/f3-clean feat/telemetry-parallel-latency
Preparing worktree (detached HEAD bf954101a)
HEAD is now at bf954101a feat(omo-senpi): emit one parallelism_summary per session

$ ln -s $MAIN/node_modules $CT/node_modules        # root
$ for each of 28 packages: ln -s $MAIN/packages/<p>/node_modules $CT/packages/<p>/node_modules
root link ok
linked agents-md-core ... linked utils    (28 links)

$ git -C $CT status --porcelain | grep -v "node_modules$"
(empty)   # ONLY node_modules provisioning links are untracked; zero source/test/doc modifications
```

**Gate 1 — telemetry suite:**

```
$ cd /Volumes/mengmotaStorage/local-workspaces/omo-wt-f3/f3-clean && bun test packages/omo-senpi/src/components/telemetry/
 136 pass
 0 fail
 480 expect() calls
Ran 136 tests across 15 files. [4.10s]
EXIT=0
```

**Gate 2 — typecheck:**

```
$ cd /Volumes/mengmotaStorage/local-workspaces/omo-wt-f3/f3-clean && bun run --cwd packages/omo-senpi typecheck
$ tsgo --noEmit -p tsconfig.json
EXIT=0
```

Both expected results met: **136 pass / 0 fail, exit 0** and **typecheck exit 0**.

### A3b. Per-file counts for this branch's five new test files

Each run individually from the clean checkout:

| test file | expected | actual | exit |
| --- | --- | --- | --- |
| `wave-assembler.test.ts` | 13 | **13 pass / 0 fail** | 0 |
| `savings-math.test.ts` | 12 | **12 pass / 0 fail** | 0 |
| `eval-classifier.test.ts` | 10 | **10 pass / 0 fail** | 0 |
| `omo-native-parallel.test.ts` | 15 | **15 pass / 0 fail** | 0 |
| `omo-native-parallel-summary.test.ts` | 14 | **14 pass / 0 fail** | 0 |
| **total** | **64** | **64** | — |

Raw:

```
=== wave-assembler.test.ts exit=0 ===
 13 pass
 0 fail
Ran 13 tests across 1 file. [164.00ms]
=== savings-math.test.ts exit=0 ===
 12 pass
 0 fail
Ran 12 tests across 1 file. [288.00ms]
=== eval-classifier.test.ts exit=0 ===
 10 pass
 0 fail
Ran 10 tests across 1 file. [204.00ms]
=== omo-native-parallel.test.ts exit=0 ===
 15 pass
 0 fail
Ran 15 tests across 1 file. [208.00ms]
=== omo-native-parallel-summary.test.ts exit=0 ===
 14 pass
 0 fail
Ran 14 tests across 1 file. [209.00ms]
```

**No test was lost in the rebase.** All five files contribute their full counts; 64/64.
The 148 -> 136 suite drop is fully explained by dev's `372bd800b` (deleted
`omo-native-buffer.ts`, gutted `omo-native-notice.test.ts`), not by this branch.

### A4. Wider blast radius — `bun test packages/omo-senpi/src/`

```
$ cd /Volumes/mengmotaStorage/local-workspaces/omo-wt-f3/f3-clean && bun test packages/omo-senpi/src/
 1545 pass
 11 fail
 4209 expect() calls
Ran 1556 tests across 231 files. [134.36s]
EXIT=1
```

11 failures. I did **not** assume they were pre-existing — I proved it with a baseline.
Every failure is a missing generated skill bundle:

```
error: ENOENT: no such file or directory, open '.../packages/omo-senpi/plugin/skills/ast-grep/SKILL.md'
error: ENOENT: no such file or directory, open '.../packages/omo-senpi/plugin/skills/give-me-tips/SKILL.md'
error: ENOENT: no such file or directory, open '.../packages/omo-senpi/plugin/skills/start-work/SKILL.md'
```

`plugin/skills` is a build artifact — only 2 of ~20 skill roots are tracked in git on either revision:

```
$ git ls-tree -r --name-only 95fe3be52 packages/omo-senpi/plugin/skills | wc -l   -> 2   (dev)
$ git ls-tree -r --name-only bf954101a packages/omo-senpi/plugin/skills | wc -l   -> 2   (branch)
```

**Baseline control — identical clean checkout of `dev` (`95fe3be52`), identical provisioning:**

```
$ cd /Volumes/mengmotaStorage/local-workspaces/omo-wt-f3/f3-devbase && bun test packages/omo-senpi/src/
 1491 pass
 11 fail
 4054 expect() calls
Ran 1502 tests across 227 files. [139.28s]
EXIT=1

$ diff <(fails from dev baseline) <(fails from branch)
IDENTICAL FAILURE SETS
```

The 11 failing tests (`OMO Senpi scoped skill sync` x7, `runSenpiInstaller source refresh` x3,
`omo-senpi ultrawork component` x1) are **byte-identical on a clean `dev` checkout**. They are
a pre-existing "tests require a build step that a fresh checkout has not run" condition,
independent of this branch. Branch delta vs dev baseline: **+54 passing tests, +4 test files,
+0 new failures.**

---

## PART B — Skill pipeline end to end

Torn-state guard: recorded md5 of the three skill files before the run and re-checked after.
`fetch_data.py 864a039ad0d0bf0c503a01cc24b00bc2`, `build_unified.py ed1adbebe8c5a98d60f88d5ef2aba523`,
`unified_model.py 3d59de5bb3cca2a7952dac73dbfbfed4` — **all three unchanged after the run, so no
re-run was needed and no torn state was observed.**

### B1. fetch_data.py

```
$ cd /Users/yeongyu/.agents/skills/omo-native-telemetry/scripts && python3 fetch_data.py /tmp/f3-data
...
parallel_savings: 30 rows
parallelism_daily: 0 rows
headline: 1 rows
per_user: 654 rows
...
EXIT=0
```

`parallelism.json` produced. Its content is the zero-data case — no `parallelism_summary`
events exist in production yet, which is expected since the branch is unshipped:

```
$ cat /tmp/f3-data/parallelism.json
[[0, null, null, null, null, null, null, null, null, null, null, null, null]]
$ cat /tmp/f3-data/parallelism_daily.json
[]
```

This is the **worst case for the card** — every metric is a raw SQL `null`. Exactly the
condition that would surface `None`/`null` on screen if the view model were sloppy.

### B2. growth_analysis.py

```
$ cd .../scripts && uv run --with numpy --with polars --with scipy python3 growth_analysis.py /tmp/f3-data
users=683 cohort=360 gini=0.743 top10%=57.8%
...
PARALLEL x CACHE (users delegs>=3, n=433): rho=0.198 p=3e-05 | cache 87.0% (par>=50%, n=296) vs 82.8% (par<50%, n=137)
OK growth_stats.json written
EXIT=0
```

### B3. build_unified.py

```
$ cd /Users/yeongyu/.agents/skills/omo-native-telemetry/templates && python3 build_unified.py /tmp/f3-data
html written 12604
EXIT=0
$ ls -la /tmp/f3-data/index_unified.html
-rw-r--r--  1 yeongyu  wheel  14220 Aug 16 18:20 /tmp/f3-data/index_unified.html
```

Declared height read from the generated HTML's own `body` rule:

```
body { font-family:...; background:#f5f4ed;
  color:#141413; width:1080px; height:3060px; overflow:hidden; ... }
```

### B4. Null-text scan of generated HTML

Tags stripped, entities unescaped, then scanned visible text:

```
visible-text null-ish hits: 0
--- raw html occurrences (incl. attrs) ---
None 0
null 0
undefined 0
NaN 0
```

Zero occurrences of `None` / `null` / `undefined` / `NaN` **anywhere in the file**, not merely
in visible text. With an all-`null` `parallelism.json` input, the view model degrades to the
Korean placeholder `수집 대기` ("awaiting collection") / `수집 전` ("pre-collection").

### B5. Headless Chrome render at the declared height

```
$ cd /tmp/f3-data && "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
    --no-sandbox --hide-scrollbars --force-device-scale-factor=1 --window-size=1080,3060 \
    --screenshot=/tmp/f3-data/f3-dashboard.png /tmp/f3-data/index_unified.html
543714 bytes written to file /tmp/f3-data/f3-dashboard.png
EXIT=0
$ python: size (1080, 3060)
```

(Chrome emitted benign macOS Keychain/`os_crypt` warnings; exit 0, PNG written.)

**Rendered PNG preserved as evidence:**

| path | contents |
| --- | --- |
| `.omo/evidence/telemetry-parallel-latency-v2/f3-render/f3-dashboard-full.png` | full 1080x3060 dashboard |
| `.omo/evidence/telemetry-parallel-latency-v2/f3-render/f3-parallelism-card.png` | crop y=1600..2350, the new card |
| `.omo/evidence/telemetry-parallel-latency-v2/f3-render/f3-footer-bottom800.png` | crop y=2260..3060, footer strip |

### B6. What I actually observed in the PNG (I looked at all three images)

**New parallelism card renders — YES.** Bottom-left of the third row, titled
`네이티브 툴콜 병렬도 — 실측 스팬 기반` ("native tool-call parallelism — measured span based").
Headline reads `모델 추정 절감  수집 대기`. Sub-line explains
`parallelism_summary 이벤트가 아직 수집되지 않음 (세션 0개) · 세션당 최대 1회 발화되며, 실측 시작/종료 시각의 스팬(Σ소요시간 − 웨이브 스팬)으로 모델 추정 절감을 계산한다`.
Three metric rows render with muted grey bars: `다중 콜 웨이브 비율`, `다중 웨이브당 합류 콜`,
`턴 측정시간 대비 모델 추정 절감`, each showing `수집 대기`. Companion card to its right,
`EVAL 버킷 · 데이터 품질`, shows `eval 단독 웨이브`, `eval 혼합 웨이브`,
`상한 참고치 (상한, 헤드라인 아님)`, `미완결/시계이상/드롭 콜`.

**No `None`/`null`/`undefined` in visible text — CONFIRMED.** Both by programmatic scan (0 hits
across the whole file) and by eye across the full-page render. Placeholders are human-readable
Korean, not language-level null leakage.

**Footer fully rendered, not clipped — CONFIRMED.** Two complete lines legible in the bottom crop:
`리프트 = Fisher exact + RR 95% CI + BH-FDR (전항목 q<0.001) · 상관 = Spearman(heavy-tail) · Gini = 턴 1+ 유저 기준 · 런치 코호트`
/ `상관관계이며 인과 아님 · 비용 및 custom 라우팅 수치는 과대 가능 · omo 실시간 집계`.
Measured programmatically: last non-background (`#f5f4ed`) pixel row is **2988** of **3060** —
**72px of clear bottom margin.** No content touches the canvas edge, so no clipping.

**Honesty labels present on screen — CONFIRMED.**
- Headline savings is labelled a model estimate: the card's own headline literally reads
  **`모델 추정 절감`** ("model-estimated savings"), and the metric row reads
  **`턴 측정시간 대비 모델 추정 절감`**. The `modeled` label is on screen, not buried in a tooltip.
- The upper bound is **not** a headline: it appears as a table row in the neighbouring
  quality card, explicitly labelled **`상한 참고치 (상한, 헤드라인 아님)`** —
  "upper-bound reference (a bound, not a headline)". It is spelled out twice as a bound and
  once as explicitly not-a-headline.
- The card also distinguishes itself from the older delegation-based card:
  `위 카드의 위임 배치 절감은 동일초 디스패치로 추정한 하한, 이 카드는 툴콜별 실측 시각으로 계산한 직접 측정치`.

Rest of the dashboard renders intact (KPI row, retention lift, cache economy, delegation
savings, cost estimate, model routing, skill loads, behavior signals). No layout breakage
from the added card.

---

## Branch head moved mid-run — re-verified

The implementers pushed while I was in Part B. Branch head advanced
`bf954101a` -> `1ac22d815`:

```
$ git log --oneline bf954101a..1ac22d815
1ac22d815 docs(evidence): record post-commit bundle verification transcript
5650eb581 build(omo-senpi): regenerate plugin bundle for parallelism telemetry

$ git diff --stat bf954101a 1ac22d815
 .../telemetry-parallel-latency-v2/ci-bundle-fix.md | 156 +++++++++
 .../plugin/extensions/omo-init-deep-advisor.js     |  30 +-
 packages/omo-senpi/plugin/extensions/omo.js        | 374 ++++++++++-----------
 3 files changed, 358 insertions(+), 202 deletions(-)
```

No `src/`, test, or doc-under-test changes — only a regenerated plugin bundle and evidence.
Rather than ship a stale verdict, I re-ran the primary gates on a **third** fresh detached
worktree at the new head, same provisioning, same non-poisoned path:

```
$ git worktree add --detach .../omo-wt-f3/f3-head 1ac22d815
HEAD is now at 1ac22d815 docs(evidence): record post-commit bundle verification transcript

$ cd .../f3-head && bun test packages/omo-senpi/src/components/telemetry/
 136 pass
 0 fail
 480 expect() calls
Ran 136 tests across 15 files. [3.81s]
TEST_EXIT=0

$ cd .../f3-head && bun run --cwd packages/omo-senpi typecheck
$ tsgo --noEmit -p tsconfig.json
TYPECHECK_EXIT=0
```

Current branch head is green on both gates.

---

## PART C — Cleanup receipt

Every worktree removed via `git worktree remove --force` (never `rm -rf` on a worktree):

```
$ git worktree remove --force /tmp/f3-clean
removed /tmp/f3-clean
$ git worktree remove --force .../omo-wt-f3/f3-clean
removed f3-clean
$ git worktree remove --force .../omo-wt-f3/f3-devbase
removed f3-devbase
$ git worktree remove --force .../omo-wt-f3/f3-head
removed f3-head + parent
$ rmdir .../omo-wt-f3
(removed)
$ rm -rf /tmp/f3-data
removed /tmp/f3-data
$ pkill -f "Google Chrome.*headless"
(rc=1 — no surviving headless process to kill; Chrome had already exited after --screenshot)
```

Verification:

```
$ ls -d /tmp/f3-clean /tmp/f3-data /Volumes/.../omo-wt-f3
ls: /tmp/f3-clean: No such file or directory
ls: /tmp/f3-data: No such file or directory
ls: /Volumes/mengmotaStorage/local-workspaces/omo-wt-f3: No such file or directory

$ ps aux | grep -i "[h]eadless"
(none)

$ git worktree list | grep -c f3
0
```

`git worktree list` no longer shows any temp tree of mine. The one remaining `/tmp` entry,
`/private/tmp/omo-preexist-debug-038ed`, is **another agent's pre-existing worktree** — it was
present in `git worktree list` before I started and I did not touch it.

One incident worth recording: during the first (`/tmp`) provisioning pass, my per-package
symlink loop nested a link at `/tmp/f3-clean/packages/lsp-daemon/node_modules/node_modules`
because that directory transiently existed mid-checkout. I detected it and removed the nested
link immediately (`rm`, a plain symlink, not a worktree), then confirmed `git status` showed
only intended provisioning entries. The subsequent authoritative runs used a loop guarded by
`[ ! -e ]` and did not reproduce it.

Repo state untouched — no source, test, or doc file was modified anywhere. The only files I
wrote are this verdict and the three evidence PNGs under `f3-render/`:

```
$ git -C .../omo-wt/feat-telemetry-parallel-latency status --porcelain
(clean apart from this evidence file and f3-render/)
```

---

## Verdict rationale

| gate | result |
| --- | --- |
| Clean-checkout `bun test .../telemetry/` = 136 pass / 0 fail, exit 0 | PASS (both revisions) |
| Clean-checkout `bun run --cwd packages/omo-senpi typecheck` exit 0 | PASS (both revisions, outside poisoned `/tmp`) |
| Branch's five new test files contribute full 64 tests (13/12/10/15/14) | PASS — exactly 64, nothing lost in rebase |
| Wider `bun test packages/omo-senpi/src/` shows no branch-caused regression | PASS — 11 failures byte-identical on clean `dev` baseline |
| Skill pipeline runs end to end and produces a rendered image | PASS — fetch/analysis/build all exit 0, 1080x3060 PNG rendered |
| New parallelism card renders | PASS — visually confirmed in PNG |
| No `None`/`null`/`undefined` in visible text | PASS — 0 occurrences in the entire HTML, under an all-null data input |
| Footer fully rendered, not clipped | PASS — last content pixel row 2988 of 3060, 72px clear margin |
| Honesty labels on screen (headline = MODEL ESTIMATE; bound labelled as bound) | PASS — `모델 추정 절감` headline; `상한 참고치 (상한, 헤드라인 아님)` as a non-headline row |
| Cleanup complete | PASS — all worktrees removed via `git worktree remove --force`, receipt above |

No gate failed. The only two failures encountered during this task were both proven
environmental (`/tmp/node_modules` `@types/node@18` shadowing; missing generated `plugin/skills`
build artifacts reproducing identically on clean `dev`), neither attributable to this branch.

Caveat carried forward, not blocking: dependency provisioning was a symlink of the existing
store, so **a from-scratch `bun install` from `bun.lock` is still unproven**. That should be
covered by CI rather than by this task, which was explicitly instructed not to run `bun install`.

```
verdict: APPROVE
```
