# F1 — Plan compliance audit (telemetry-parallel-latency-v2)

```
verdict: APPROVE
```

Auditor: independent F1 task `st_01a009d6`, depth 1. Read-only with respect to source: no source,
test, doc, or skill file was modified. Only this file was written. No `bun install` was run.

Scope basis: `git diff origin/dev HEAD` post-rebase — 7 commits ahead, 0 behind, 21 files,
+3499/-0.

---

## 0. Baseline, confirmed by me (not taken on report)

| Claim | Command | Result |
|---|---|---|
| telemetry suite green | `bun test packages/omo-senpi/src/components/telemetry/` | `136 pass / 0 fail / 480 expect() calls, Ran 136 tests across 15 files` |
| typecheck green | `bun run --cwd packages/omo-senpi typecheck` | `$ tsgo --noEmit -p tsconfig.json`, exit 0, no output |
| wave-assembler | `bun test .../wave-assembler.test.ts` | 13 pass / 0 fail |
| savings-math | `bun test .../savings-math.test.ts` | 12 pass / 0 fail |
| eval-classifier | `bun test .../eval-classifier.test.ts` | 10 pass / 0 fail |
| omo-native-parallel | `bun test .../omo-native-parallel.test.ts` | 15 pass / 0 fail |
| omo-native-parallel-summary | `bun test .../omo-native-parallel-summary.test.ts` | 14 pass / 0 fail |
| **new tests total** | sum | **64**, exactly as briefed |

### The 148 → 136 drop is dev's, not a lost test — verified independently

- `git show --stat 372bd800b` (a `dev` commit, not on this branch): deletes
  `omo-native-buffer.test.ts` (-176) and `omo-native-buffer.ts` (-86), and cuts
  `omo-native-notice.test.ts` by -173/+... — that is where the 12 tests went.
- `git diff --name-only origin/dev HEAD -- 'packages/.../telemetry/*.test.ts'` lists exactly 7
  files: the 5 new ones plus `omo-native-component.test.ts` and `product-identity.test.ts`.
- `git diff origin/dev HEAD -- <those two> | grep -c '^+  test('` → **2** added tests.
- `git diff origin/dev HEAD -- '.../telemetry/*.test.ts' | grep -c '^-.*test('` → **0** deleted.
- Arithmetic closes: 70 pre-existing (dev) + 64 new files + 2 additive = **136**. This branch
  removed zero tests.

---

## 1. Todo-by-todo acceptance criteria

### Todo 1 — `wave-assembler.ts` (13 tests, all pass)

| # | Criterion | Verdict | Deciding evidence |
|---|---|---|---|
| (a) | 3 overlapping calls → 1 wave, size 3, span produced | PASS | `wave-assembler.test.ts:41` — `expect(waveShape(result)).toEqual([{ size: 3, spanMs: 600, maxConcurrency: 3 }])` |
| (b) | 3 sequential calls → 3 waves of size 1 | PASS | `wave-assembler.test.ts:56-60` — three `{ size: 1, spanMs: 100, maxConcurrency: 1 }` entries |
| (c) | overlap + sequential mix → correct split | PASS | `wave-assembler.test.ts:74-77` — `[{size:2,spanMs:300},{size:1,spanMs:50}]` |
| (d) | missing end → incomplete = 1, excluded from metrics | PASS | `wave-assembler.test.ts:104-107` — `counters.incomplete === 1`, waves length 1, only `"done"` present |
| (e) | `endMs < startMs` → clock_anomaly, excluded | PASS | `wave-assembler.test.ts:118-121` — `clockAnomalies === 1`, `pairedCalls === 1`, only `"sane"` in the wave |
| (f) | > 2000 calls → detail dropped, counters retained | PASS | `wave-assembler.test.ts:138-141` (`trackedCalls === MAX_TRACKED_CALLS`, `droppedCalls === 10`, `observedCalls === 2010`); plus the reopen-repair cases at `:158-161` (all-starts-first shape, drop 500) and `:174-179` (unmatched starts, four-sink accounting) |
| (g) | chained A(0-5) B(4-9) C(8-12) → 1 wave, span 12 | PASS | `wave-assembler.test.ts:87-89` — `waves` length 1, `spanMs === 12`, `maxConcurrency === 2` |
| MUST NOT | not grouped by turn | PASS | `wave-assembler.ts:98-119` `groupIntoWaves` operates purely on interval reach; no turn concept exists in the module |
| MUST NOT | arrays not unbounded (cap = 2000) | PASS | `wave-assembler.ts:16` `MAX_TRACKED_CALLS = 2000`; `:74` gate is `paired.length + pending.size >= MAX_TRACKED_CALLS` — the reopen defect (pending map excluded from the gate) is genuinely fixed in `eb623928c` and pinned by the two tests above |

Sweep detail worth recording: `sweepMaxConcurrency` (`wave-assembler.ts:135-152`) sorts ends
before starts at equal timestamps, so a touching pair is concurrency 1 — asserted at
`wave-assembler.test.ts:238-240`.

### Todo 2 — `savings-math.ts` (12 tests, all pass)

| # | Criterion | Verdict | Deciding evidence |
|---|---|---|---|
| (a) | simultaneous `[(0,2.0),(0,2.2),(0,1.9),(0,2.1)]` → modeled 6.00 | PASS | `savings-math.test.ts:69` — `toBeCloseTo(6.0, 10)`, label asserted `"modeled"` at `:68` |
| (b) | long tail `[(0,.3)x3,(0,9.0)]` → modeled 0.90, upper 7.43, values differ | PASS | `savings-math.test.ts:81,83,84` — `0.9`, `7.43`, and `expect(upper.valueMs).not.toBeCloseTo(modeled.valueMs, 2)` |
| (c) | N = 1 → 0 | PASS | `savings-math.test.ts:94-96` — modeled 0, upper 0, roundTrips 0 |
| (d) | N = 0 → 0 | PASS | `savings-math.test.ts:106-109` — includes `savedRoundTrips([])` |
| (e) | negative result NOT clamped | PASS | `savings-math.test.ts:126` — `expect(modeledWallClockSavedMs(wave).valueMs).toBe(-8)` |
| (f) | chained `[(0,5),(4,9),(8,12)]` → modeled 2.00, explicitly not 9.00 | PASS | `savings-math.test.ts:136-137` — `toBeCloseTo(2.0,10)` **and** `not.toBe(9.0)`. This is the B1 regression line and it is present verbatim |
| (g) | chained `maxConcurrency = 2` → `savedRoundTrips = 1`, not N−1 = 2 | PASS | `savings-math.test.ts:143-145` — `maxConcurrency === 2`, `savedRoundTrips === 1`, and `not.toBe(calls.length - 1)` |
| contract | span formula, never `max(dᵢ)` | PASS | `savings-math.ts:46` — `sum(durations) - wave.spanMs`. The only `Math.max` in the file is `savings-math.ts:60` `Math.max(wave.maxConcurrency - 1, 0)`, i.e. round-trip flooring, not a savings basis |
| contract | `(N−1)×mean` exposed only as `_upper_bound` | PASS | `savings-math.ts:50-54` is the sole such computation and returns `label: "upper_bound"`; the type `UpperBoundSavedMs` (`:41-44`) is structurally distinct from `ModeledSavedMs` (`:36-39`), so a caller cannot substitute one for the other without a visible cast |
| MUST NOT | no ratio-mean, no median | PASS | `grep -rni "median" packages/omo-senpi/src/components/telemetry/*.ts` → no hits |

### Todo 3 — `eval-classifier.ts` (10 tests, all pass)

| # | Criterion | Verdict | Deciding evidence |
|---|---|---|---|
| (a) | `[bash,read,grep]` → non_eval | PASS | `eval-classifier.test.ts:17` |
| (b) | `[eval]` → eval_only | PASS | `eval-classifier.test.ts:21-22` (also `[eval,eval]`) |
| (c) | `[bash,eval]` → mixed | PASS | `eval-classifier.test.ts:26` |
| (d) | naming variants match; `evaluate_foo` does NOT | PASS | `eval-classifier.test.ts:30-35` — positives `eval, codemode, mcp:eval, code-mode, EVAL, " eval ", server/eval, tool_eval, code_mode`; negatives `evaluate_foo, evaluate, ln, bash, read, codemodel, eval_helper, "", "   "`. The plan's explicit "`ln` must not match" is asserted |
| (e) | mixed never enters non_eval aggregates | PASS | `eval-classifier.test.ts:39-44` — with a mixed wave present, `nonEval.wavesTotal === 1`, `mixedWaves === 1` |
| (f) | `waves_total`/`waves_multi`/`joined_calls`/`wave_size_histogram` all non_eval-only | PASS | `eval-classifier.test.ts:48-70` — polluted vs clean input compared field-by-field on **all four** counters including the histogram string, then pinned to absolute values `3 / 2 / 6 / "1:1:1:0:0:0:0:0"`. Adding eval waves cannot change any of them |
| MUST NOT | eval not filtered-then-merged | PASS | `eval-classifier.ts:64-70` — `eval_only` and `mixed` both `continue` before the non_eval accumulation block; no recompute-after-removal path exists |
| MUST NOT | no eval cell source/args transmitted | PASS | The module's only input is `toolNames: readonly string[]` + `spanMs` (`eval-classifier.ts:19-22`) |

Note (not a criterion failure): the plan said "reuse the existing `matchesToolName` suffix matcher".
`eval-classifier.ts:96-104` **copies** it rather than importing from `omo-native-tools.ts:182-190`.
I diffed the two bodies — they are logically identical (same normalization, same four match forms),
so the *matching contract* the criterion actually names is satisfied and case (d) proves the
semantics. Flagging as a duplication concern for F2, not as an unmet acceptance criterion.

`code_mode` in `EVAL_TOOL_NAMES` (`eval-classifier.ts:29`) is not scope expansion: `normalizeToolName`
maps `code-mode` → `code_mode`, so it is the same identifier family the plan named, not a new tool.

### Todo 4 — `omo-native-parallel.ts` (15 tests, all pass)

| # | Criterion | Verdict | Deciding evidence |
|---|---|---|---|
| (a) | start/end pairs feed the assembler into waves | PASS | `omo-native-parallel.test.ts:70-73` — 3 dispatched pairs → 1 wave, `spanMs === 600`, tool names `[bash, read, grep]`, `pairedCalls === 3` |
| (b) | session state emptied after `session_shutdown` | PASS | `omo-native-parallel.test.ts:196-197` — `registry.size() === 0` and `snapshot(...)` undefined; `:204-206` proves only the shutting-down session is dropped; `:229-230` proves a late end/turn_end cannot resurrect it (`omo-native-parallel.ts:56` — only a `start` may `ensure()` a session) |
| (c) | unknown payload shapes do not throw | PASS | `omo-native-parallel.test.ts:239-268` — 13 malformed payloads dispatched to all four handlers; no throw, zero waves, zero turn duration. `:271-278` adds the unresolvable-session case |
| turn timing | `turn_start.timestamp` used, `turn_end` stamped on arrival, duration accumulates | PASS | `omo-native-parallel.test.ts:118` — two turns → `measuredTurnDurationMsTotal === 1_000`; `:126-128` no-start case → 0; `:136` reversed clock → 0 |
| MUST NOT | does not capture events itself | PASS | `omo-native-parallel.ts` has no capture/transport import; it returns a registry only |
| MUST NOT | does not store `args`/`result` | PASS | `omo-native-parallel.test.ts:85-86` — the retained call equals exactly `{toolCallId,toolName,startMs,endMs}` and `JSON.stringify(snapshot)` does **not** contain the `"do-not-store"` sentinel planted in both `args` and `result` |

### Todo 5 — `parallelism_summary` schema registration

| # | Criterion | Verdict | Deciding evidence |
|---|---|---|---|
| suite green incl. `schema-doc.test.ts` | PASS | `bun test .../schema-doc.test.ts .../product-identity.test.ts .../omo-native-component.test.ts` → `16 pass / 0 fail`, with `#then the generated schema block is byte exact` passing. Doc regenerated: `docs/reference/senpi-telemetry.md` gains 16 rows in the diff |
| histogram ≤ 64 chars at the real cap | PASS | `product-identity.test.ts:110-124` — worst case `8 buckets × "2000"` joined by `:` = 39 chars, asserted `toHaveLength(bucketCount*4 + 7)` and `toBeLessThanOrEqual(64)`; also asserts the property is registered as `string` and present in the allowlist |
| placed after `feature_used` | PASS | `product-identity.ts:135` (`feature_used` block) → `:135+` `parallelism_summary` block, matching the plan's `:131-135` anchor |
| `non_eval_` prefix on every tool-call-derived count | PASS | `product-identity.ts:145-149` — `non_eval_joined_calls`, `non_eval_saved_round_trips`, `non_eval_wave_size_histogram`, `non_eval_waves_multi`, `non_eval_waves_total`. No unprefixed wave/call count exists |
| honesty labels in names | PASS | `modeled_wallclock_saved_ms`, `upper_bound_saved_ms`, `measured_turn_duration_ms_total` all present at `product-identity.ts:141-144` |
| MUST NOT | no median / mean-ratio property; no `_text`/`_path`/`_prompt` suffix; no label in histogram | PASS | full property list read at `product-identity.ts:136-151`; histogram is positional, asserted label-free at `eval-classifier.test.ts:80` (`not.toContain("=")`) and `omo-native-parallel-summary.test.ts:220` |

One deviation from the plan's literal property list, judged acceptable: `dropped_calls` was added
beyond the enumerated set (`product-identity.ts:139`). It is a quality counter of exactly the same
class as the plan's own `incomplete_calls`/`clock_anomalies`, it exists because todo 1's cap can now
refuse calls, and without it the four-sink accounting
(`paired + incomplete + dropped + anomalies == observed`) cannot close — see
`omo-native-parallel-summary.test.ts:238-241`. It is documented in task-5 and task-6 evidence, is in
the allowlist and the regenerated doc, and carries no domain-ambiguity (it is not a `non_eval`
metric — it counts refusals before classification). This is an additive quality counter, not a new
event and not a forbidden metric shape, so it does not violate "no scope expansion: no new events".

### Todo 6 — session summary emission (14 tests, all pass)

| # | Criterion | Verdict | Deciding evidence |
|---|---|---|---|
| suite + typecheck green | PASS | 136 pass / 0 fail; typecheck exit 0 (section 0) |
| (a) | one session → exactly 1 emission | PASS | `omo-native-parallel-summary.test.ts:104-114` — `captured` length 1, name `parallelism_summary`, payload matches `non_eval_waves_total:1, non_eval_joined_calls:2, non_eval_saved_round_trips:1, modeled_wallclock_saved_ms:300, schema_kind:"parallelism_v1"`. Second shutdown emits nothing: `:264-265` |
| (b) | all buckets zero → 0 emissions | PASS | `omo-native-parallel-summary.test.ts:250` (idle), `:258` (only incomplete/anomalous calls), `:270` (turn with no tool calls) — all `expect(fx.captured).toEqual([])`. Gate at `omo-native-parallel-summary.ts:80-82` requires all three buckets zero |
| (c) | every payload key inside the allowlist | PASS | `omo-native-parallel-summary.test.ts:126-128` — `Object.keys(properties).sort()` equals the full `OMO_NATIVE_PROPERTY_ALLOWLISTS.parallelism_summary` sorted (exact-set equality, not subset); repeated through the real transport at `omo-native-component.test.ts:217-219` with `SHARED_KEYS` |
| (d) | `turn_completed` emission count unchanged | PASS | `omo-native-component.test.ts:140-147` — the pre-existing exact event-stream assertion (`daily_active, session_started, omo_senpi_daily_active, prompt_submitted, turn_completed, feature_used`) is **byte-unchanged** in the diff, so any extra or missing `turn_completed` fails it; `omo-native-component.test.ts:220` additionally pins zero `turn_completed` in the new parallel scenario. `git diff origin/dev HEAD -- omo-native-turns.ts product-identity.ts \| grep turn_completed` → no hits: neither the emitter nor the schema was touched |
| (e) | real registration order — emission is not a no-op | PASS | `omo-native-component.test.ts:184-222` drives the **real** `createOmoNativeTelemetryComponent(...).register(...)` with a recording transport and asserts exactly 1 non-empty `parallelism_summary` at `:210`. Ordering enforced at `omo-native-component.ts:61-65` (registered before the session component, with the reason in a comment). task-6 evidence records the mandatory mutation: moving the registration after the session component makes this test fail `Expected length: 1 / Received length: 0` while the 13 unit tests stay green — proving the test pins the criterion the unit suite structurally cannot |
| volume bound | 1 per session, never per turn/wave | PASS | the only `captureEvent` call site is inside the `session_shutdown` handler (`omo-native-parallel-summary.ts:48-57`); snapshot is consumed and the session cleared in the same handler |
| eval kept separate at emission | PASS | `omo-native-parallel-summary.ts:74-77` — only `non_eval`-classified waves enter `nonEvalWaves`, and savings sum over that array alone. Asserted end-to-end at `omo-native-parallel-summary.test.ts:170-178` (eval-only + mixed present → `non_eval_waves_total:1`, `eval_only_waves:1`, `eval_only_duration_ms:700`, `mixed_waves:1`) and `:190-196` (mixed-only session → `modeled_wallclock_saved_ms:0`, `upper_bound_saved_ms:0`) |
| MUST NOT | `turn_completed` path untouched | PASS | see (d) |

### Todo 7 — skill query bank

| # | Criterion | Verdict | Deciding evidence |
|---|---|---|---|
| `fetch_data.py <dir> --only parallelism` exits 0 and writes JSON | PASS | I ran it: `cd ~/.agents/skills/omo-native-telemetry/scripts && python3 fetch_data.py /tmp/f1-qa --only parallelism` → `parallelism: 1 rows`, `EXIT=0`, and `/tmp/f1-qa/parallelism.json` (77 bytes) exists |
| aggregates the named columns with exact schema property names | PASS | `fetch_data.py:69` — `sum(...non_eval_saved_round_trips)`, `sum(...modeled_wallclock_saved_ms)`, `sum(...non_eval_waves_total)`, `sum(...non_eval_waves_multi)`, `sum(...eval_only_waves)`, `sum(...mixed_waves)`, `count() sessions`. Each name matches `product-identity.ts:136-151` exactly |
| empty-data safe | PASS | live run returned `1 rows` of nulls (non-grouped aggregate) with exit 0; `unified_model.py:51-57` gates on that shape |
| MUST NOT | `upper_bound_saved_ms` not a headline | PASS | it is aliased `upper_bound_saved_ms_ref` at `fetch_data.py:69` and rendered only as a side kv row labelled "상한 참고치 (상한, 헤드라인 아님)" at `build_unified.py:90` |
| MUST NOT | no ratio-of-means | PASS | every aggregate is a raw `sum()`/`count()`; ratios are formed as `sum(num)/sum(den)` in `unified_model.build_parallelism` |

### Todo 8 — dashboard card + docs (repo-external; judged from the skill files + task-8.md)

| # | Criterion | Verdict | Deciding evidence |
|---|---|---|---|
| `build_unified.py <datadir>` exits 0 | PASS | full pipeline run by me: `python3 run_dashboard.py --data-dir /tmp/f1-render --no-upload` → `"queries": 41`, `"render_height": 3060`, `"png": "/tmp/f1-render/dash_unified.png"`, `PIPE_EXIT=0`. (Direct `build_unified.py` needs `growth_stats.json` from `growth_analysis.py`; that ordering is a pre-existing pipeline property, not a branch defect.) |
| footer intact in the bottom 800px after render | PASS | I decoded `dash_unified.png` (2160×6120 device px = 1080×3060 CSS at DPR2) and scanned rows against the true page background `(245,244,237)`: **last non-background device row = 5963 → CSS 2981.5**, leaving **78 CSS px of clear space** below content. No clipping. `body{height:3060px}` at `build_unified.py:134` |
| card renders with the `modeled` label on screen | PASS | `build_unified.py:73` headline `모델 추정 절감 …`, `:76-77` subtitle states the span formula `(Σ소요시간 − 웨이브 스팬)` and "측정된 벽시계 시간이 아니라 모델 추정치"; no-data branch `:96-99` keeps the same labelling |
| eval bucket shown as its own row | PASS | `build_unified.py:88-89` — `eval 단독 웨이브` and `eval 혼합 웨이브` kv rows, plus `:94` side headline `eval 버킷 …개` |
| `SKILL.md` documents definitions / formula / honesty labels / eval isolation | PASS | `SKILL.md:39` (property inventory), `:116-127` (event, emission bound, `modeled_wallclock_saved_ms = Σdᵢ − span` with the "modeled" labelling rule), `:133` (`upper_bound_saved_ms = (N−1)×mean`, "a BOUND, never an estimate"), `:142` (eval isolation rationale), `:153-154` (the `measured_`/`modeled_`/`upper_bound_` naming contract), `:159-161` (zero-data one-row-of-nulls trap), `:199` (height/clipping note) |
| MUST NOT | no card deleted or reordered | PASS | the new card is appended; the pre-existing `병렬 실행 × 캐시` and `병렬 툴콜링 절감` cards are still present at `build_unified.py:185-196` |
| reopen defect 1 (stale on-screen line) | RESOLVED | `grep -n "미계측" build_unified.py` → **no hits**. The footnote at `build_unified.py:189` now reads "…일반 툴콜/eval 병렬도는 맨 아래 실측 스팬 카드에서 직접 측정", consistent with the new card |
| reopen defect 2 (inaccurate evidence claim) | RESOLVED | `grep -c 2150 scripts/run_dashboard.py` → 0; the height is parsed from the CSS (`run_dashboard.py:59-63`) and passed to Chrome (`:68`); task-8.md now records the corrected fact with the original risk struck through rather than deleted |

Todo 8's checkbox is still `- [ ]` in the plan. The work itself meets every stated criterion under
my own re-verification, so this is a bookkeeping state, not a compliance gap.

---

## 2. Plan "Success criteria" section, line by line

| Success criterion | Verdict | Deciding evidence |
|---|---|---|
| `bun test .../telemetry/` (incl. `schema-doc.test.ts`) + typecheck green | PASS | 136 pass / 0 fail; `schema-doc.test.ts` byte-exact test passes; typecheck exit 0 |
| `docs/reference/senpi-telemetry.md` regenerated, byte-exact with the generated block | PASS | 16 `parallelism_summary` rows added in the diff; `schema-doc.test.ts` `#then the generated schema block is byte exact` passes |
| exactly 1 `parallelism_summary` per session, not a no-op from registration order, all properties allowlisted | PASS | `omo-native-component.test.ts:210` (real path, exactly 1), `:217-219` (exact allowlist key set), mutation proof in task-6.md:127-166 |
| eval waves never enter `non_eval_*` metrics incl. histogram, pinned by test | PASS | `eval-classifier.test.ts:48-70` (all four counters incl. the histogram string), `omo-native-parallel-summary.test.ts:170-196` (end-to-end) |
| default savings = `Σdᵢ − span`; chained regression (2.00) exists; `(N−1)×mean` only under `_upper_bound` | PASS | `savings-math.ts:46`; `savings-math.test.ts:136-137`; `savings-math.ts:50-54` + `product-identity.ts:150` (`upper_bound_saved_ms`) |
| round-trip savings use `maxConcurrency`, chained wave not mistaken for parallel | PASS | `savings-math.ts:57-63`; `savings-math.test.ts:143-145` |
| local skill renders the new metrics with no bottom clipping | PASS | pipeline exit 0 at height 3060; my own pixel scan: content bottom CSS 2981.5, 78px clear margin |
| all Must-NOT items verifiable as 0 occurrences (F4's job, spot-checked here) | PASS (spot-check) | `git diff --name-only origin/dev HEAD \| grep -E "telemetry-core\|omo-codex\|omo-opencode"` → no hits; `turn_completed` absent from the diff of `omo-native-turns.ts`/`product-identity.ts`; no `median` in new sources; no codemode-K/compression metric; the only `Math.max` in `savings-math.ts` is round-trip flooring, not a savings basis; every wave/call count property carries the `non_eval_` prefix; histogram asserted label-free. Full enumeration is F4's deliverable |

---

## 3. Evidence completeness

| Todo | File | Present | RED / failing-first proof |
|---|---|---|---|
| 1 | `task-1.md` (304 lines) | yes | **Honest stashed RED.** `task-1.md:21-23` states plainly that the implementation was written first and then `mv`-ed aside to obtain a genuine failing run — "This is a stashed RED, **not** an untouched-first-run RED. Nothing was fabricated." Verbatim capture with `RED_EXIT=1` at `:26-41`, plus a second independent RED (`Expected: 9, Received: 7`) at `:44-45`. This is exactly the disclosure the plan's honesty rule requires |
| 2 | `task-2.md` (224 lines) | yes | Genuine first-run RED, `:35-38` — "before any implementation existed … no `git stash` was involved" |
| 3 | `task-3.md` (217 lines) | yes | `:31-33` "Tests were written before the production module. Verbatim RED capture", plus a second failing capture at `:95-96` for the naive-filtering guard |
| 4 | `task-4.md` (207 lines) | yes | `:21` genuine RED (module absent, "No stash was needed"), plus a second RED at `:47-63` for the session-resurrection leak found in adversarial probing |
| 5 | `task-5.md` (233 lines) | yes | `:37-39` RED of the doc gate itself — schema added, doc not yet regenerated, `schema-doc.test.ts` fails. Exactly the failure the plan predicted |
| 6 | `task-6.md` (300 lines) | yes | No literal first-run RED heading, but carries a **stronger** artifact: the mandatory mutation at `:127-166` shows the ordering regression test failing (`Expected length: 1 / Received length: 0`) under the exact production defect while the 13 unit tests stay green. For a wiring/ordering criterion this is the only proof that discriminates, and it is captured verbatim. Accepted |
| 7 | `task-7.md` (256 lines) | yes | Not a TDD unit (SQL query bank, repo-external). Carries the required diff plus a relaxed-filter proof at `:149+` that distinguishes "valid query, no data" from "query silently matches nothing" — the correct falsification for this artifact type |
| 8 | `task-8.md` (584 lines) | yes | Not a TDD unit (renderer). Carries before/after height measurements at `:278-296` including a **recorded real clipping failure** (3021px content clipped at 3000px, "footer line 2 cut, VISUALLY CONFIRMED") — the plan's own required failure scenario. Reopen section documents both defects fixed and re-verified, with the false risk statement struck through in place rather than deleted |

All eight evidence files exist under `.omo/evidence/telemetry-parallel-latency-v2/`. Six carry a
literal RED capture; the two that do not (6, 8) carry the stronger discriminating artifact
appropriate to their change type (mutation proof; measured clipping failure). No fabricated
first-run RED was found: todo 1, the one case where the RED was obtained by stashing, says so in
plain language at the top of its Commands section.

Independent verification files (`verify-t1-t3.md`, `verify-t2.md`, `verify-t4.md` … `verify-t8.md`,
`verify-t1-repair.md`) are also present, plus sibling final-wave files `f2.md` and `f4.md`.

---

## 4. Findings that do not block

1. **`matchesToolName` duplicated, not imported** — `eval-classifier.ts:96-104` vs
   `omo-native-tools.ts:182-190`. Logically identical; the acceptance criterion (matching
   semantics, case (d)) is met. Hand to F2 as a DRY question.
2. **`dropped_calls` beyond the plan's enumerated property list** — justified, allowlisted,
   documented, and load-bearing for the four-sink accounting; not a new event and not a forbidden
   metric shape. Recorded above rather than treated as a silent addition.
3. **Todo 8's plan checkbox is still unchecked** — bookkeeping only; the work verifies clean.

None of these is an unmet acceptance criterion.

---

## Verdict

**APPROVE.** All eight todos meet every acceptance criterion stated in the plan, verified against
the shipped code (file:line) and against command output I ran myself rather than against the
workers' claims. Every one of the plan's Success criteria holds. All eight evidence files exist and
each carries either a RED capture or an honest, explicitly-labelled account of how the failing state
was obtained. No criterion was reworded, weakened, or quietly dropped.
