Issue 7392 manual QA
====================

Surface
-------
Built Codex ulw-loop component CLI:
  node components/ulw-loop/dist/cli.js hook pre-tool-use-spawn

Help
----
`--help` printed the supported hook and ulw-loop command usage.

Bad input
---------
Malformed JSON input:
  exit=0
  output=<empty>

Repeated reviewer spawns
------------------------
Input agent type:
  lazycodex-code-reviewer

Observed:
  attempt=1 output=<allow>
  attempt=2 output=<allow>
  attempt=3 output=<allow>
  attempt=4 permissionDecision=deny
  reason includes:
    reviewer no-progress cap reached
    4/3
    goal g1 attempt 1

Persisted state:
  review-spawn-counts.json:
    lazycodex-code-reviewer:g1:a1 = 3
  spawn-count.json:
    count = 3

This proves the denied fourth reviewer does not consume the global fan-out
budget.

MultiAgentV2 message-only roles
-------------------------------
The built hook CLI was driven without agent_type. The message named each role:

  lazycodex-code-reviewer:
    attempts 1-3: allow
    attempt 4: deny with lazycodex-code-reviewer 4/3

  lazycodex-qa-executor:
    attempts 1-3: allow
    attempt 4: deny with lazycodex-qa-executor 4/3

Persisted state:
  each reviewer count = 3
  global spawn count = 6

Mixed-role gate priority
------------------------
Input message:
  Act as lazycodex-gate-reviewer; audit the lazycodex-code-reviewer report

Observed:
  permissionDecision=deny
  reason names missing .omo/evidence/g1-code-review.md
  lazycodex-gate-reviewer:g1:a1 = 1
  lazycodex-code-reviewer:g1:a1 remained 3 from the earlier dedicated test

This proves a mixed prompt uses the restrictive gate path instead of consuming
the code-reviewer quota.

Real Codex app-server
---------------------
The project Codex QA driver installed the local plugin into an isolated
`CODEX_HOME`, used a local mock model, and completed one app-server turn.

Observed plugin hook events:
  sessionStart: completed
  userPromptSubmit: completed
  stop: completed

The app-server run was repeated after the MultiAgentV2 role-detection change
with the same completed event set.

It was repeated again after mixed-role gate prioritization with the same
completed event set and isolation result.

Isolation:
  real ~/.codex/config.toml unchanged
  isolated CODEX_HOME removed after the run
  no real model API call
